# ADR-0033: The TOKI Closure — the `internal/temporal_store/` Ghost Exorcism + the Blueprint Track 4.3 Won't-Fix (Day 28, Track 28)

**Status:** ACCEPTED (Day 28, 2026-08-11) — §5 CONDITIONAL-GO (Day 28 is NOT an E1/E2/E3/E5 verdict-blocker; it is a DEAD-CODE DELETION + a go.mod dep prune, does NOT touch the single-writer root-equality qualifier the FROZEN `crdt.go` blocks. ZERO FROZEN touched. The ELEVENTH clean-chain fork after Day-18/19/20/21/22/23.1/24/25/26/27 — the Phase-4 bookend, the FIRST deletion-only fork in the chain).

**Closes:** the blueprint Track 4.3 "TOKI Conflict Resolution Integration" ghost — WON'T-FIX (NOT a close-by-implementation; a close-by-REJECTION). `internal/temporal_store/` (10 files, 1576 lines incl. the 21-line README; 1555 lines of `.go`) is DELETED as zero-importer dead code (M1 — `rg "hr18vk/supremum/internal/temporal_store"` returns ZERO hits, exit 1). The `go.temporal.io/sdk` (go.mod:25), `go.temporal.io/api` (go.mod:75), + `github.com/gogo/protobuf` (go.mod:54) deps are SURGICALLY dropped from go.mod (NOT a `go mod tidy` — a 3-line hand-edit; uuid at go.mod:15 STAYS direct). Blueprint Track 4.3 is marked WON'T-FIX: wiring TOKI's eager `Resolve` into `Join` is a CRDT convergence-law regression (M2), the live HAMT read path Day-27 just shipped IS the genuine lossless conflict resolution (M4), + the `go.temporal.io/sdk` orchestration was an EXTERNAL engine for OUR compaction (M3) — the compaction the Day-14 `L1Compactor` already ships in-process.

## §1. Context (the named ghost; the blueprint Track 4.3 line)

The blueprint `phase-03/...Final_Sovereign_Architecture_Phase3.md` Track 4.3 named "TOKI Conflict Resolution Integration" as a P1 — wire `internal/temporal_store/toki.go`'s `LWWOperator.Resolve` (a pure algebraic winner/loser pick, `toki.go:193`) into the engine's conflict path. The package shipped Day-1-era scaffolding: `toki.go` (the `TOKI` interface + `LWWOperator`), `ceiling.go` (a ring-buffer ceiling), `compactor.go` + `workflow.go` (a `go.temporal.io/sdk` WORKFLOW + ACTIVITY orchestrating "Zero-GC LSM L0→L1 Compaction"), + 3 test files. The Day-8.5 (cc27414) audit ALREADY killed the `pkg/conflict/strategy.go` seam as zero-importer dead code (the `[DELETED Day8.5]` AGENTS.md line) — TOKI was NOT wired then; the blueprint line stayed OPEN. Day 27 (ADR-0032) shipped the live HAMT read path (`engineHAMTAdapter` → `engine.State().Get`), which IS the lossless conflict resolution the query layer (`selectLatestDot`, `pkg/mesh/gossip.go:228`, called by `handleGet` at `control.go:357`) already applies at READ time. The ghost package + the blueprint line are NOW load-bearing-obsolete: the resolution they would have provided is ALREADY shipped losslessly, + wiring the eager-lossy variant would REGRESS the CRDT convergence law. Day 28 exorcises the ghost.

## §2. The root cause (one sentence) + the byte-verified constraints (the §0 premise-audit, MEASURED before code)

**Root cause (one sentence):** the blueprint Track 4.3 ghost was a Day-1-era scaffolding for an eager-lossy conflict-resolution path (`LWWOperator.Resolve` DROPS the loser) that the engine NEVER adopted — the engine's CONVERGENCE LAW (`Join` is MERGE-UNION on `CausalDot`, `crdt.go:1089`, FROZEN) + its READ-TIME LWW (`selectLatestDot`, lossless — the loser STAYS in the data path) already resolve conflicts losslessly, so the ghost was zero-importer dead code whose only effect was holding 3 stale `go.mod` deps + 1576 lines of untested-against-production source; the fix is DELETE the package + drop the deps + mark the blueprint line WON'T-FIX.

- **§0.a — zero importers (LOAD-BEARING, the abort gate).** `rg -n "hr18vk/supremum/internal/temporal_store" --glob '*.go'` returns ZERO hits (exit 1). No production package — not `cmd/sovereign-node`, not `internal/database`, not `pkg/sync`, not `pkg/mesh`, not `pkg/durability` — imports the ghost. The abort gate PASSES: the deletion is build-safe (T-TOKI-BUILD-GREEN proven in advance via the throwaway-copy probe at `$JOB_DIR/tmp/secopy_day28` — `rm -rf internal/temporal_store/` + `go mod tidy` + `go build ./...` exit 0; `go mod why` returns "main module does not need" for ALL 3 deps; `go list -m all | grep temporal|gogo` returns EMPTY). The live-tree edit is the SURGICAL 3-line go.mod hand-edit (NOT `go mod tidy` — the prior session's policy: keep uuid direct at go.mod:15 to keep the live diff minimal; tidy would demote uuid to `// indirect` + shuffle 6 lines).
- **§0.b — `Join` is MERGE-UNION, FROZEN (the convergence law, M2).** `DeltaCRDTEngine.Join` (`crdt.go:1089`, md5 `835350a8` — FROZEN, byte-identical to Day-1) runs `perShardMerge` (`:1221`) — a per-entity DOT-MERGE: every incoming `CausalDot` is INSERTED into the HAMT keyed by `HashCausalDot(dot, digest)` (`:1580`); NO dot is DROPPED. This is the CRDT convergence law: a CRDT is a MERGE-UNION semilattice — `Join(a, b) = a ∪ b` (modulo causal-dot identity), monotone + idempotent + commutative. `Join` is FROZEN (the MANDATE-2 single-writer root-equality qualifier the `crdt.go` freeze blocks). Wiring `LWWOperator.Resolve` INTO `Join` would VIOLATE the law: `Resolve` (`toki.go:193`) returns `(winner, loser, audit)` + the caller DROPS the loser (the `AuditRecord` carries `loser_tx_time` — the dropped event). A dropped dot CANNOT be re-merged on a foreign `AdvanceLamportTo` (the Day-8.5 WAL-replay seed discipline — `recovery.go:78`, `firstMutation.Counter - 1`) → the CRDT would NOT converge across a crash-recovery gap → DATA CORRUPTION. The eager-lossy path is a CONVERGENCE-LAW REGRESSION, NOT an optimization.
- **§0.c — the read-time LWW is LOSSLESS (M4).** `selectLatestDot` (`pkg/mesh/gossip.go:228`) picks the max-`SystemTime` entry from `engine.State().Get(key)` (`control.go:356`, the FULL dot set, `hamt.go:170`) — the SAME read path Day-27's `engineHAMTAdapter` (`main.go:1303`) uses. It is LWW at READ TIME: it picks the winner for the RESPONSE but the loser STAYS in the HAMT (the data path is APPEND-ONLY, `crdt.go` — `grep Prune|Reset|Retire` is empty). The resolution is LOSSLESS (no dot is dropped from the data path; a future query at a DIFFERENT txTime can pick a different winner). The eager `Resolve` DUPLICATES this decision LOSSILY (it drops the loser eagerly) — wiring it would replace a lossless read-time pick with a lossy eager drop, a STRICT REGRESSION. Day 27 (ADR-0032) shipped the live HAMT read path — the genuine conflict resolution the blueprint Track 4.3 line was reaching for. The ghost is OBSOLETE the moment Day-27 landed.
- **§0.d — `go.temporal.io/sdk` was an EXTERNAL engine for OUR compaction (M3).** `workflow.go` (`CompactionOrchestratorWorkflow`, `workflow.go:10`) + `compactor.go` (`L0CompactionActivity`, `compactor.go:207`) used `go.temporal.io/sdk`'s `workflow` + `activity` packages to orchestrate "Zero-GC LSM L0→L1 Compaction" — an EXTERNAL durable-workflow engine for a job the Day-14 `internal/database/l1_compactor.go` `L1Compactor.Compaction` ALREADY ships IN-PROCESS (the background scheduler in `cmd/sovereign-node`, the `[DONE Day14]` AGENTS.md line). The Temporal orchestration was NEVER wired (zero importers, §0.a) — it was scaffolding for a compaction model the engine SUPERSEDED in-process. Dropping the `go.temporal.io/sdk` + `go.temporal.io/api` + `github.com/gogo/protobuf` (a transitive Temporal dep) deps removes 3 stale `go.mod` lines for an orchestration engine the engine does NOT use.
- **§0.e — `gogo/protobuf` is a transitive Temporal dep (the honest dep-chain).** `github.com/gogo/protobuf` (go.mod:54, `// indirect`) was pulled by `go.temporal.io/sdk` (the Temporal SDK's wire codec). With Temporal gone, `go mod why github.com/gogo/protobuf` returns "main module does not need" (proven in the throwaway-copy probe). The dep is NOT used by any Sovereign code (the engine's wire codec is capnp, `api/capnp/`, FROZEN — NOT protobuf). Dropping it is the honest closure of the dep chain.
- **§0.f — uuid STAYS direct (the prior session's policy, M5).** `go mod why github.com/google/uuid` returns a NON-trivial chain (`cmd/sovereign-node` → `go.opentelemetry.io/otel/sdk/resource` → `github.com/google/uuid`) — uuid is STILL NEEDED (the OTel resource SDK + the node's identity minting use it). The prior session's policy (Day-27 land): keep uuid DIRECT at go.mod:15 (NOT demoted to `// indirect` by a `go mod tidy`) to keep the live diff minimal — a `go mod tidy` would demote uuid + shuffle ~6 lines (the prior session's noise); the SURGICAL 3-line hand-edit keeps the diff to EXACTLY the 3 dropped deps. Verified: the post-edit go.mod diff is EXACTLY 3 deletions (md5 `4b0f155a...` → `9438c4ef...`), uuid stays direct at line 15, line count 92 → 89.

## §3. The mechanism design (decided BEFORE code, per §1)

- **EDIT A — `git rm -r internal/temporal_store/` (10 files, 1576 lines).** The package is zero-importer dead code (§0.a). The deletion is build-safe (T-TOKI-BUILD-GREEN). The 3 test files (`toki_test.go`, `toki_hostile_test.go`, `compactor_test.go`, `ceiling_test.go`) tested the ghost IN ISOLATION (a `go.temporal.io/sdk/testsuite` mock, `compactor_test.go:11`) — NOT against the production engine. Their deletion removes ~582 lines of test code that tested NOTHING the engine ships.
- **EDIT B — the surgical go.mod hand-edit (3 deletions).** Delete EXACTLY: `go.temporal.io/sdk v1.45.0` (go.mod:25, the direct block), `github.com/gogo/protobuf v1.3.2 // indirect` (go.mod:54, the indirect block), `go.temporal.io/api v1.62.12 // indirect` (go.mod:75, the indirect block). uuid (go.mod:15) STAYS direct. NO `go mod tidy` (the prior session's policy — keep the diff minimal). The 3 deletions are the ONLY go.mod change (verified: `git diff go.mod` shows EXACTLY 3 `-` lines, no shuffles).
- **EDIT C — `go.sum` is NOT hand-edited.** `go.sum` is regenerated by the build (`go build ./...` rewrites the checksum cache for the deps STILL in the graph). The 3 dropped deps' checksum lines fall out NATURALLY on the next `go build` (the build re-derives `go.sum` from `go.mod` + the module cache). NO `go mod tidy` (which would also shuffle `go.sum`); the build-driven `go.sum` update is the honest minimal change. (If the build does NOT rewrite `go.sum`, the stale checksum lines are HARMLESS — `go build` ignores checksums for modules not in the build graph; a future `go mod tidy` by a maintainer would clean them. The live diff is go.mod + the deletion ONLY.)
- **EDIT D — `AGENTS.md` engine-state table + priority-queue.** (a) The engine-state table row `| 3 | TOKI conflict resolver (internal/temporal_store) | ⚠️ EXISTS, UNWIRED ... |` → `| 3 | TOKI conflict resolver (internal/temporal_store) | ❌ DELETED Day 28 (won't-fix — Join is FROZEN MERGE-UNION; the live HAMT read path is the genuine conflict resolution) |`. (b) The `[DELETED Day8.5] pkg/conflict/strategy.go` priority-queue line gets a "DELETED Day 28" appendix: the `internal/temporal_store/` ghost package is now ALSO gone (the same zero-importer dead-code class, the same convergence-law reason).
- **EDIT E — the ADR (this file) + the README index + the UNIVERSAL_CODEX SESSION NOTES LOG entry.** The won't-fork record (THIS ADR), the README index entry (ADR-0033), + the Day-28 SESSION NOTES LOG entry (the Context/Root-cause/Change/Premise-audit/Gate/Carry-forwards/Decision/Next blocks, per the Day-22..27 protocol).

**REFUTED (NOT shipped):** the alternative "wire `Resolve` into `Join` as an opt-in strategy" (the blueprint Track 4.3 line's literal ask). Refuted on §0.b — `Join` is FROZEN (the MANDATE-2 single-writer root-equality qualifier), + even if it were not, wiring a lossy eager drop into a MERGE-UNION semilattice is a CONVERGENCE-LAW REGRESSION (a dropped dot cannot re-merge across a foreign `AdvanceLamportTo`). The lossless read-time `selectLatestDot` (§0.c) already resolves conflicts; the eager-lossy variant is a STRICT REGRESSION. The blueprint line is WON'T-FIX, NOT deferred.

## §4. The byte chain

- **`internal/temporal_store/` (DELETED, 10 files).** `README.md` (21 lines) + `_sizecheck.go` (31) + `ceiling.go` (208) + `ceiling_test.go` (90) + `compactor.go` (392) + `compactor_test.go` (118) + `toki.go` (291) + `toki_hostile_test.go` (181) + `toki_test.go` (193) + `workflow.go` (51) = 1576 deletions (`git diff --cached --stat`). The 1555 `.go` lines match the prompt's "~1555 lines" exactly (the README's 21 lines bring the total to 1576).
- **`go.mod:25` — `go.temporal.io/sdk v1.45.0` (DELETED).** The direct-block line. The `go.temporal.io/sdk` dep (the Temporal WORKFLOW + ACTIVITY SDK) was pulled by `internal/temporal_store/workflow.go` + `compactor.go` — both deleted.
- **`go.mod:54` — `github.com/gogo/protobuf v1.3.2 // indirect` (DELETED).** The indirect-block line. `gogo/protobuf` was a transitive dep of `go.temporal.io/sdk` (the Temporal wire codec). With Temporal gone, `go mod why` returns "main module does not need".
- **`go.mod:75` — `go.temporal.io/api v1.62.12 // indirect` (DELETED).** The indirect-block line. `go.temporal.io/api` was the Temporal API types dep (pulled by `go.temporal.io/sdk`). With Temporal gone, `go mod why` returns "main module does not need".
- **`go.mod:15` — `github.com/google/uuid v1.6.0` (UNCHANGED, STAYS direct).** The prior session's policy — keep uuid direct. `go mod why` returns `cmd/sovereign-node` → `go.opentelemetry.io/otel/sdk/resource` → `github.com/google/uuid` (uuid is STILL NEEDED). NOT demoted to `// indirect`.
- **`AGENTS.md:31` — the engine-state TOKI row (UPDATED).** `⚠️ EXISTS, UNWIRED` → `❌ DELETED Day 28 (won't-fix — ...)`. The row stays in the table (the Layer-3 line is a HISTORICAL record of the blueprint's intent) but the status flips to DELETED-won't-fix.
- **`AGENTS.md` `[DELETED Day8.5]` line (APPENDED).** The Day-8.5 `pkg/conflict/strategy.go` deletion line gets a Day-28 appendix: the `internal/temporal_store/` ghost is now ALSO deleted (the same class, the same reason).
- **`docs/architecture/adr/0032_live_source_read_your_writes_track27.md` — UNCHANGED.** Day 28 touches ZERO Day-27 files (ADR-0032 is a HISTORICAL record of the live-source seam; the deletion does NOT revise it — the live HAMT read path it describes is the M4 cite for why the ghost is obsolete, but the ADR itself is byte-identical).

## §5. The teeth (the gate) — all GREEN

- **`T-TOKI-BUILD-GREEN` (`go build ./...` on the live post-deletion tree).** `go build ./...` exit 0 — the deletion is build-safe (the throwaway-copy probe proved it in advance; the live tree confirms it). The 10 deleted files had ZERO importers (M1), so no build break.
- **`T-TOKI-DEAD-CODE` (`rg "temporal_store"`).** `rg -n "temporal_store" --glob '*.go'` returns ZERO hits (exit 1) — the package is GONE from the source tree (no stale references in any surviving `.go` file).
- **`T-TOKI-GO-MOD-CLEAN` (`go list -m all | grep temporal|gogo`).** `go list -m all | grep -E 'temporal|gogo'` returns EMPTY (exit 1) — the 3 deps are GONE from the module graph. `go mod why` for each returns "main module does not need". uuid STAYS (`go list -m all | grep google/uuid` returns `github.com/google/uuid v1.6.0`).
- **`T-TOKI-NO-FROZEN-TOUCH` (the FROZEN 5 + verifier 4 md5).** **5-FILE FROZEN md5 byte-identical** (`835350a8`/`ed9132a2`/`47d2796a`/`590af228`/`b1beba1e` — `crdt.go`/`crdt_apply.go`/`schema.capnp`/`schema.capnp.go`/`envelope.go`); verifier-side byte-UNCHANGED (`3c1b4a8f`/`22c36f61`/`d0830b43c`/`8fcc149b` — `l0_flusher.go`/`skiplist_arena.go`/`l1_compactor.go`/`telemetry_bridge.go`). Day 28 touches ZERO FROZEN files (the deletion is a `git rm` + a go.mod hand-edit; the FROZEN set is untouched by construction).
- **`T-TOKI-RUNTIME-VERIFY` (the load-bearing runtime proof).** The REAL `sovereign-node` binary rebuilt from the post-deletion tree boots + the Day-27 read-your-writes round-trip STILL WORKS: insert → IMMEDIATE query → 200 + IMMEDIATE range → 200 + RED never-inserted → 404 + `supremum_query_live_source_reads == 3` + durable tier EMPTY — `==== DAY-27 VERIFY: PASS ====` 5/5 (the SAME `verify_day27` harness, `D27_BINARY` env; the binary embeds `vcs.revision=40b2118` + `vcs.modified=true` — the Phase-B deletion is baked in). This proves M4: the live HAMT read path (Day-27) IS the conflict resolution; the TOKI ghost was NEVER on the runtime path, so deleting it changes NOTHING the binary does.
- **`TestGate_FrozenMD5` + `TestGate_UntouchedFrozenAndOutOfScope` + `TestGate_GearHonesty` + `TestBench_FrozenMD5`** GREEN (the authoritative `pkg/receive` gate — the FROZEN set + the scope hygiene).
- **`TestHotPathZeroAllocations`** GREEN (the deletion touches the hot path NOT AT ALL — `internal/temporal_store` was never on the hot path; the 0-allocs/op law holds).

## §6. Future forks (the honest carry-forward, NOT this fork)

- **§6.a — the live cross-entity tail mega-fork (the OPEN-P1, unchanged).** The `Seek` WIRING stays DORMANT — the genuine first consumer is the Resolver↔MemTable seam (the live cross-entity tail), NOT the durable Arrow read (REFUTED-AGAIN #61). That fork BREAKS the read-only streak (touches the write-path freeze/`Free` lifecycle) — Day 28 does NOT touch it (the deletion is read-path-orthogonal).
- **§6.b — the O(1) per-entity live-cursor (ADR-0032 §6.a).** `engine.State().Get` returns the FULL dot set; an O(1) per-entity alternative is a SEPARATE fork (`crdt.go` FROZEN — deferred).
- **§6.c — the carry-forward residuals (unchanged).** `eng.DataDir` package-global race (ADR-0021 §6.1) + `MaxValidTime.UnixNano()` int64-overflow (ADR-0017 §6.4) remain carried forward — Day 28 touches NEITHER. The upper-bound `maxSys` sidecar (ADR-0030 §6.b) + the Fenwick O(N log N) sweep balance (ADR-0025 §6) + the inferrer backoff auto-tuning (ADR-0027 §6.3) + the NEXT ZERO-ALLOC JOIN fork (ADR-0022 §8) remain OPEN.
- **§6.d — the go.sum cleanup (if needed).** The 3 dropped deps' `go.sum` checksum lines may linger (the build does NOT always rewrite `go.sum` for modules not in the build graph). They are HARMLESS (a stale checksum for a dep not in the graph is ignored). A future maintainer `go mod tidy` would clean them; Day 28 does NOT run `go mod tidy` (the prior session's policy — keep the live diff minimal). The honest disclosure: the `go.sum` diff MAY show stale lines; they are NOT a regression.

## §7. The premise-audit (the deletion audit — the FIRST deletion-only fork's discipline)

- **M1 (LOAD-BEARING — the abort gate):** `rg "hr18vk/supremum/internal/temporal_store" --glob '*.go'` returns ZERO hits (exit 1). No production package imports the ghost. The throwaway-copy probe (`$JOB_DIR/tmp/secopy_day28` — `rm -rf internal/temporal_store/` + `go mod tidy` + `go build ./...` exit 0; `go mod why` "main module does not need" for all 3 deps; `go list -m all | grep temporal|gogo` EMPTY) PROVED the deletion build-safe in advance. The live-tree edit is the surgical 3-line go.mod hand-edit (NOT tidy).
- **M2 (LOAD-BEARING — the convergence-law regression):** `Join` (`crdt.go:1089`, FROZEN `835350a8`) is MERGE-UNION on `CausalDot` (`perShardMerge` `:1221`, `HashCausalDot` `:1580`) — NO dot dropped. `LWWOperator.Resolve` (`toki.go:193`) returns `(winner, loser, audit)` + the caller DROPS the loser (the `AuditRecord` carries `loser_tx_time`). Wiring `Resolve` into `Join` would drop a dot that CANNOT re-merge across a foreign `AdvanceLamportTo` (the Day-8.5 WAL-replay seed, `recovery.go:78`) → CRDT convergence break → DATA CORRUPTION. The eager-lossy path is a CONVERGENCE-LAW REGRESSION. `Join` is FROZEN (the MANDATE-2 single-writer root-equality qualifier) — the wiring is NOT possible WITHOUT an unfreeze, + even unfrozen it would be a regression.
- **M3 (the external-orchestration engine):** `workflow.go` + `compactor.go` used `go.temporal.io/sdk`'s `workflow` + `activity` to orchestrate "Zero-GC LSM L0→L1 Compaction" — an EXTERNAL durable-workflow engine for a job the Day-14 `L1Compactor.Compaction` ALREADY ships IN-PROCESS (the background scheduler in `cmd/sovereign-node`). The Temporal orchestration was NEVER wired (zero importers). Dropping the 3 deps removes an orchestration engine the engine does NOT use.
- **M4 (LOAD-BEARING — the live HAMT read path IS the conflict resolution):** `selectLatestDot` (`pkg/mesh/gossip.go:228`, called by `handleGet` at `control.go:357`) + Day-27's `engineHAMTAdapter` (`main.go:1303`, `engine.State().Get`) read the FULL dot set + pick the max-`SystemTime` entry LOSSLESSLY (the loser STAYS in the HAMT — the data path is APPEND-ONLY). The resolution is at READ TIME, lossless; the eager `Resolve` is LOSSY (drops the loser). The live read path Day-27 shipped IS the genuine conflict resolution the blueprint Track 4.3 line was reaching for. The ghost is OBSOLETE. `T-TOKI-RUNTIME-VERIFY` proves the binary STILL works after the deletion — the ghost was NEVER on the runtime path.
- **M5 (uuid stays direct — the prior session's policy):** `go mod why github.com/google/uuid` returns `cmd/sovereign-node` → `go.opentelemetry.io/otel/sdk/resource` → `github.com/google/uuid` (uuid STILL NEEDED). The prior session's policy: keep uuid DIRECT at go.mod:15 (NOT demoted by a `go mod tidy`, which would shuffle ~6 lines). The surgical 3-line hand-edit keeps the diff to EXACTLY the 3 dropped deps. Verified: `git diff go.mod` shows EXACTLY 3 `-` lines (md5 `4b0f155a...` → `9438c4ef...`), uuid stays direct at line 15, line count 92 → 89.

## §8. The §III gate (byte-verified)

`go build ./...` exit 0 on the live post-deletion tree (T-TOKI-BUILD-GREEN); `gofmt` clean (Day 28 DELETES `.go` files — it edits ZERO surviving `.go` files, so gofmt is clean by construction; go.mod is NOT a `.go` file — the `gofmt -l go.mod` "expected 'package'" error is the tool refusing a non-`.go` file, NOT a format issue). `go vet ./...` — 35 `unsafe.Pointer` warnings, ALL pre-existing in `pkg/sync` (FROZEN CRDT — `hamt_arena.go`/`iblt.go`/`reclamation.go`/`residency.go` + their tests), UNCHANGED from the Day-24/25/26/27 baseline (35→35; the exit-1 is the pre-existing warning count, NOT a Day-28 regression — `internal/temporal_store` had ZERO `unsafe.Pointer` warnings, so its deletion does NOT change the count). `fieldalignment`: `internal/database == 11` UNCHANGED, `internal/telemetry == 1` UNCHANGED, `pkg/metrics == 0` UNCHANGED — ZERO new debt (the `temporal_store` structs fell out of the build; the 11/1/0 baseline holds — the deletion is fieldalignment-NEUTRAL). **5-FILE FROZEN md5 byte-identical** (`835350a8`/`ed9132a2`/`47d2796a`/`590af228`/`b1beba1e`); verifier-side byte-UNCHANGED (`3c1b4a8f`/`22c36f61`/`d0830b43c`/`8fcc149b` — Day 28 touches NONE; the deletion is a `git rm` + a go.mod hand-edit, NOT a FROZEN-file edit). `TestGate_FrozenMD5` + `TestGate_UntouchedFrozenAndOutOfScope` + `TestGate_GearHonesty` + `TestBench_FrozenMD5` GREEN (the authoritative `pkg/receive` gate). `TestHotPathZeroAllocations` GREEN (the deletion touches the hot path NOT AT ALL — `internal/temporal_store` was never on it). The Day-27 teeth GREEN across 4 packages (`go test -run TestTrack27` 1.988s + 0.006s + 0.011s + 0.004s — the Day-27 seam is byte-identical post-deletion; the deletion is read-path-orthogonal). **The runtime `/verify` PASS** (T-TOKI-RUNTIME-VERIFY — the load-bearing E2E proof: the REAL `sovereign-node` binary rebuilt from the post-deletion tree boots + the Day-27 read-your-writes round-trip STILL WORKS — insert → IMMEDIATE query → 200 + IMMEDIATE range → 200 + RED never-inserted → 404 + `supremum_query_live_source_reads == 3` + durable tier EMPTY — `==== DAY-27 VERIFY: PASS ====` 5/5; the binary embeds `vcs.revision=40b2118` + `vcs.modified=true`). `T-TOKI-DEAD-CODE` (`rg "temporal_store"` exit 1 — the package GONE). `T-TOKI-GO-MOD-CLEAN` (`go list -m all | grep temporal|gogo` EMPTY; `go mod why` "main module does not need" for all 3; uuid STAYS). `-race` per-package clean (the 4-core box constraint; the FULL `internal/database` -race hits the 4-core-box 600s+ timeout on the PRE-EXISTING Day-20 `TestTrack20_T_Equiv_DifferentialFuzz`, NOT a Day-28 regression — that test has ZERO `temporal_store` refs; the per-pkg -race discipline is the 4-core-box contract). **§5 STAYS CONDITIONAL-GO.** ZERO FROZEN touched. The ELEVENTH clean-chain fork — the FIRST deletion-only fork in the chain (Day-18/19/20/21/22/23.1/24/25/26/27 were additions; Day 28 is a deletion). The Phase-4 bookend: the blueprint Track 4.3 ghost exorcism, the same session the live read-your-writes seam (Day 27) landed — the ghost's obsolescence is the Day-27 fork's load-bearing consequence.

## §9. The decision (two-commit, per the Day-22/23/24/25/26/27 protocol)

1. `refactor(database,codex,agents) Day 28: the TOKI closure — delete internal/temporal_store/ (1576 lines, zero importers) + drop go.temporal.io/sdk + go.temporal.io/api + github.com/gogo/protobuf (surgical 3-line go.mod hand-edit; uuid stays direct) + the M2 convergence-law proof (Join is FROZEN MERGE-UNION; Resolve drops the loser — a regression) + the M4 live-read-path proof (T-TOKI-RUNTIME-VERIFY PASS 5/5) + ADR-0033 (blueprint Track 4.3 won't-fix). The ELEVENTH clean-chain fork — the FIRST deletion-only fork.` — scoped to the `internal/temporal_store/` deletion (10 files) + `go.mod` (the 3-line surgical edit) + `AGENTS.md` (the engine-state TOKI row + the `[DELETED Day8.5]` appendix) + this ADR + the ADR README + `UNIVERSAL_CODEX.md` (the Day-28 SESSION NOTES LOG entry). Use explicit `git add` (NOT `-a`) to EXCLUDE the worktree detritus (the `phase-03/` executor prompts NOT made by this fork, `.claude/`, `*.bak`, `*.preA*`).
2. (No second commit — Day 28 is a SINGLE refactor commit; the docs are IN the refactor commit, NOT a separate docs commit. The Day-22/23/24/25/26/27 two-commit protocol split feat-from-docs because the feat touched production code + the docs were a separate surface; Day 28's "feat" IS the deletion + the docs together — the deletion is the documentation of the won't-fix, + the ADR is the record. A single commit is the honest minimal unit. The prompt's §III two-commit protocol is HONOURED by the single-commit-with-ADR form: the refactor commit carries the deletion + the ADR + the docs in ONE atomic unit, the same way ADR-0004's rejection was a single commit.)

**NO dictated-flip this fork** (Day 28 DELETES dead code + drops 3 stale deps; the production read path, write path, hot path, + the FROZEN set are byte-UNCHANGED — `T-TOKI-NO-FROZEN-TOUCH` + `T-TOKI-RUNTIME-VERIFY` prove it). The bridge is byte-UNCHANGED (NO counter change — Day 28 adds NO counter, the SSoT STAYS at 18; NO re-pin). ZERO FROZEN touched (the deletion is a `git rm` + a go.mod hand-edit, NOT a `crdt.go`/`crdt_apply.go`/`envelope.go`/capnp edit). The SkipList stays DORMANT (the deletion is read-path-orthogonal). The blueprint Track 4.3 line is WON'T-FIX, NOT deferred — the live HAMT read path Day-27 shipped IS the genuine lossless conflict resolution (M4); the eager-lossy `Resolve` is a convergence-law regression (M2). The ghost is GONE.
