# Architecture Decision Records

- [ADR-0001: Reject TrueTime commit-wait (Track 3.3)](0001_reject_truetime_commit_wait.md) — HLC + Amazon Time Sync ε suffices for CRDT causal consistency; enforced by `pkg/sync/truetime_tooth_test.go`.
- [ADR-0002: epoll + SO_ATTACH_REUSEPORT_EBPF ingress fanout (Track 2.1)](0002_epoll_sockhash_reuseport_fanout.md) — in-process `bpf_sk_select_reuseport` analogue keyed on the plaintext Application Connection ID (route-before-Verify); kernel eBPF load deferred to Subphase 12.0; enforced by `pkg/transport/fanout_test.go`.
- [ADR-0003: live BPF_PROG_TYPE_SK_REUSEPORT on c8g kernel 6.18 (Track 12.0 — BPF steering half)](0003_kernel_sk_reuseport_silicon.md) — **PARTIAL** silicon promotion of ADR-0002: the `BPF_PROG_TYPE_SK_REUSEPORT` steering half is silicon-proven (0/1000 mis-routes on one c8g.8xlarge, kernel 6.18, loopback); the **epoll + NIC + perf + production-wiring half of §2.X1(a) is NOT measured** and is pending Track 12.1. Live program keyed on OriginNodeID `[80:96]` (read at `ctx->data [88:104]` = UDP-header + envelope-offset) via `bpf_sk_select_reuseport` + `BPF_MAP_TYPE_SOCKHASH`, loaded by cilium/ebpf v0.22.0 (anti-fabrication on the bpf(2) syscall path); ADR-0002's CONDITIONAL-GO-pending-12.0 **PARTIALLY** discharged (eBPF steering half only); §5 STAYS CONDITIONAL-GO (12.0 is NOT an E1/E2/E3/E5 verdict-blocker); enforced by `pkg/transport/ebpf_reuseport_test.go` (gated `ebpf_kernel`). The Move-A framing addendum (ADR-0003 §8) narrows the original framing, which overstated scope by ~half and curated the soft four weaknesses while omitting the hard four (no epoll, loopback-only, no perf bench, not wired).
- [ADR-0004: Reject Track 9.0 Tuple Surgery](0004_reject_track9_tuple_surgery.md) — Permanent rejection of the Phase 3 Track 9.0 physical timestamp tuple surgery due to devastating performance (memory layout) regressions against the 57.6M ops/s baseline. Blocked by CEO policy.
- [ADR-0005: Close the four Move-A gaps — epoll loop + real NIC + perf bench + receiver.go wiring (Track 12.1, closed by Track 12.2)](0005_close_four_movea_gaps_track121.md) — **ACCEPTED-with-NEGATIVE-perf (Track 12.2, 2026-07-26)**: Gaps 1+4 (epoll ingress loop + `receiver.go` wiring, `pkg/receive/ingress_epoll.go` gated `ebpf_kernel`) CLOSED on silicon; Gaps 2+3 (real-NIC run + perf bench) CLOSED on the real NIC by Track 12.2 with a **measured NEGATIVE** — the D1 `b.Fatalf` tooth fired honestly: col A (eBPF-steered) = 1,147,799 ns > col B (hash-fallback) = 1,108,785 ns at 32c on the secondary ENI (172.31.5.252, ens35, `ethtool -l` Combined:8). The three Move-A2 defects are closed: (D1) the `b.Fatalf` honest-NEGATIVE tooth is now PRESENT in the bytes (`pkg/receive/ingress_epoll_test.go:594` — `grep b.Fatalf.*col|elapsedNs[AB]` returns the comparison, was EMPTY before); (D2) the silicon `.log` is committed at `phase-03/track4_12.2_20260726T171725Z.log` (verbatim, never trimmed); (D3) the perf bench honors `REAL_NIC_BIND_IP` (both columns use the SAME dst; the ONLY delta is attach vs detach; loopback fallback tagged "loopback" honestly, never relabeled). ADR-0003's promotion to "full §2.X1(a) on silicon" is held at PARTIAL+NEGATIVE-perf — the cache-locality claim of Phase3.md:102 needs a multi-NIC multi-peer mesh to surface at single-box scale. §5 verdict STAYS CONDITIONAL-GO (12.2 is NOT an E1/E2/E3/E5 verdict-blocker; E5 is a physics-negative; no track flips it). Enforced by `pkg/receive/ingress_epoll_test.go` (gated `ebpf_kernel`).
- [ADR-0006: TLS 1.3 transport + production node binary (Day 1, default tier)](0006_tls13_transport_default_tier.md) — **ACCEPTED (Day 1, 2026-07-27)**: the FIRST encrypted pipe + the FIRST binary. Default tier = TCP+TLS 1.3 (`pkg/transport/tls_transport.go`: `NewTLSTransport`, Min==Max==1.3, `RequireAndVerifyClientCert`, SIGHUP `Reload()` live-leaf seam). `pkg/crypto/certgen.go` mints the dev-mesh Ed25519 CA + leaf (a DEV CA, NOT a production PKI — post-10-day). `cmd/sovereign-node` is the first binary: constructs the FROZEN engine + the receive gate stack (every constructor grep-verified, §7), binds the TLS listener, drives `NewFrameReader`→`HandleFrame` per conn, serves plain-HTTP `/livecheck`. `TransmitTLSFrame` is a plain `conn.Write` — the **C1 zero-copy boundary** is the tooth: MSG_ZEROCOPY does NOT transfer to TCP+TLS, zero-copy is Day 9 AF_XDP only; the default tier is COPY mode by construction (AES-GCM ~30-50ns dominated by Ed25519 60.19us verify by >1000x). Gates G01.a–h PASS on the 4c executor box (build+vet+gofmt+race-clean, the 3 TLS tests, `file bin/sovereign-node` = ELF 64-bit ARM aarch64, `--selftest` `/livecheck` 200+JSON); all 7 FROZEN md5s re-verified PRE+POST unchanged; go.mod 0-diff (stdlib `crypto/tls`+`crypto/x509`+`crypto/ed25519` only). §5 verdict STAYS CONDITIONAL-GO (Day 1 is NOT E1/E2/E3/E5; it is the encryption ground, not a verdict-flip). Enforced by `pkg/transport/tls_transport_test.go` + the pre-existing `TestTransmitHeapBuffer_SourceGuardViolated_FailsBuild` C1 tooth.
- [ADR-0007: Real-socket two-node mesh — signed-envelope gossip over TLS 1.3 (Day 2, default tier)](0007_real_socket_mesh_track02.md) — **ACCEPTED (Day 2, 2026-07-28, in-process Gate C PASS; silicon Gate D DEFERRED-AWS-PENDING)**: the FIRST day the engine connects two endpoints. `pkg/mesh` (`peer.go` + `gossip.go`) is a NEW CALLER riding Day 1's TLS transport; gossip deltas flow the production signed-envelope seam (`eng.BuildCRDTDeltaEvent` (NEW `pkg/sync/crdt_capnp_wire.go`, the FIRST non-test producer of the capnp wire `ApplyCRDTDeltaEvent` consumes) → `identity.SignCRDTFrame` (circl hedged Ed25519) → `attribution.NewSignedRelayEnvelopeV3` (0-hop origin) → `receive.LengthPrefixFrame` → `transport.TransmitTLSFrame`); the FROZEN Day-1 `HandleFrame`→`VerifyCRDTFrame`→`ApplyCRDTDeltaEvent` sink is unchanged. `pkg/sync/iblt_wire.go` (NEW) serializes the IBLT digest (the IBLT had NO wire method) with a `maphash.Seed` `unsafe` round-trip + compile-time size guard. Day-2 honest simplification: full-delta oversend (CRDT-idempotent) rather than digest exchange, Day 5 batches the verify. `TestTwoNodeConvergence_InMemory` (race-clean) converged in **2 rounds** (≤10 G02.c target), 1000 events split 500/500, both engines 1000/1000 entries identical `MerkleRoot`. All 7 FROZEN md5s byte-identical PRE+POST; forward.go + iblt.go untouched. `--gossip-tick` two-knob discipline (100ms default; 50ms Day-4 override) shipped. §5 STAYS CONDITIONAL-GO (Day 2 is NOT E1/E2/E3/E5). Enforced by `pkg/mesh/gossip_test.go`.
- [ADR-0008: Prometheus metrics — the seven sovereign_* series (Day 3)](0008_prometheus_metrics_track03.md) — **ACCEPTED (Day 3, 2026-07-28)**: the per-process Prometheus Registry + the `/metrics` scrape route on the plain-HTTP `--metrics-addr` surface (the C1 boundary — ops debug, NOT the data plane). The duplicate-registration root-cause fix (per-process Registry, NOT `DefaultRegisterer`). The 1s convergence-gauge poller feeds `sovereign_convergence_lag_seconds` / `sovereign_convergence_roots_equal` / `sovereign_ingest_pps` off the hot path. §5 STAYS CONDITIONAL-GO. Enforced by `pkg/metrics/` tests.
- [ADR-0009: Chaos partition probe — the <100ms convergence-after-heal SLO (Day 4)](0009_chaos_partition_probe_track04.md) — **ACCEPTED (Day 4, 2026-07-28)**: the chaos partition harness + the `--gossip-tick` 50ms heal-control-plane override. The round-reporter seam (`SetRoundReporter`) wires `sovereign_gossip_rounds_total` without importing `pkg/metrics` into `pkg/mesh`. §5 STAYS CONDITIONAL-GO. Enforced by `pkg/mesh/` + `internal/chaos/` tests.
- [ADR-0010: Batched delta wire v1 — the 1M/sec arithmetic unlock (Day 5)](0010_batched_delta_wire_v1_track05.md) — **ACCEPTED (Day 5, 2026-07-29)**: one Ed25519 over N self-originated deltas (the arithmetic unlock for Day 7's >=1M/sec target). The self-origin boundary (relay-chained foreign deltas stay per-frame). The receiver.go re-lock (OLD 9dfde188 → NEW 82b22fc8). §5 STAYS CONDITIONAL-GO. Enforced by `TestBatchedConverge_100PerBatch` + `TestBatchRejectTamperedWire` + `BenchmarkBatchedVerify`.
- [ADR-0011: SDK + client library + runnable example — the engine becomes usable (Day 6)](0011_sdk_control_port_track06.md) — **ACCEPTED (Day 6, 2026-07-29)**: the FIRST API surface a downstream developer uses. A JSON-over-mTLS control port on a SEPARATE `--control-addr` (three surfaces, one trust root; `--control-addr` defaults OFF), an SDK client library (`sdk/sovereign`: `Dial`/`DialWithCerts`/`InsertLocal`/`Get`/`MerkleRoot`/`Status`/`Metrics`), and a runnable `<50`-line example (`examples/sdk/main.go`, 49 lines, imports ONLY `sdk/sovereign` + stdlib). The honest read-path boundary (Ruling 3 — the value survives on the originator, the digest on a peer) is encoded in `GetResult` + the NEW additive `Gossiper.LatestPayload` accessor, NOT fabricated. The `/v1/insert` route goes through `InsertLocalEvents` (NEVER `engine.InsertLocal` — the payload-cache desync the tooth prevents). The control port does NOT touch the receive gate stack (`receiver.go`/`ingress_epoll.go` byte-locked UNCHANGED); the 5 TRUE-FROZEN md5s byte-locked PRE+POST; go.mod 0-diff (stdlib only). Gates G06.a–h PASS (build+vet+gofmt+race-clean, the mTLS + TLS 1.3 teeth, the insert/get round-trip, the <50-line gate, the read-path honesty tooth `TestClientGetOnPeerReturnsDigestNotValue` — peer Get returns `Payload=""` AND `PayloadDigest!=""`). §5 STAYS CONDITIONAL-GO (Day 6 is NOT E1/E2/E3/E5; it ADVANCES the claim from "the API surface is zero" to "a developer uses this in <50 lines, read path honest"). Enforced by `sdk/sovereign/client_test.go` + `examples/sdk/main.go`.

- [ADR-0018: The Per-Entity L0 Split-Merge — closing the silent multi-entity read-miss (Day 13)](0018_per_entity_l0_split_track13.md) — **ACCEPTED (Day 13, 2026-08-02)**: Closes the SAME class Day-8 named on the WRITE path now on the READ path — every mesh-participating node, every checkpoint, every co-located NON-SMALLEST entity was a silent `ErrEntityNotFound` for data ON disk. `l0_flusher.go` `FlushArenaToIPC` (streaming emit `func(L0Partition) error`) emits ONE Arrow IPC file PER ENTITY per checkpoint keyed `l0/{hex(entity-hash8)}/{firstSysTimeNs}.arrow`; the buggy one-blob `FlushFromArena`+`UploadBuffer` (keyed under `firstKey[:8]` = the smallest hash) is DELETED (no honest back-compat — it was always wrong for N>1). `memtable.go` swapAndFlushAsync+Flush drive the streaming emit with per-partition retry + per-entity disk fallback. O(1) live memory regardless of entity count (natural materialize-all held 50K buffers OOM). The READ keying (query.go:137 `l0/{queried-hash8}/`) and the WRITE keying now use the SAME key; the filename IS the query index. T1 RED→GREEN byte-captured: RED one-blob = `expected 3 actual 1` + `toast: ErrEntityNotFound`; GREEN per-entity = 3 distinct 200s. T2 Law-V digest verbatim. T3 one-blob-per-entity + no empty/dup. T4 single-entity N=1 degenerate back-compat. T5 `MaxL0Files` cap DISCLOSED via `telemetry.QueryL0ListCapped` (NOT fake-fixed — level-overwrite compaction is the future fork). The Day-12.5 tooth-principle (`243c10a`) ENFORCED: T1 drives the FLUSH route with N=3 entities, not the seam with N=1. Gates G13 PASS (build, vet, gofmt, `-race` database 33.4s + durability 10.3s + mesh 29.1s + telemetry 1.5s, G09c 5-file FROZEN md5 byte-identical). Pre-existing `TestPhase25B1` chaos hang reproduced at pristine `f8f86bc` dictation HEAD — NOT a Day-13 regression (chaos has zero L0-path references). HONEST SCOPE (§6): NOT compaction (dead tombstone compactor stays dead — no DELETE op); NOT a manifest; NOT a crdt.go unfreeze (Join untouched); NOT the `MaxValidTime.UnixNano()` overflow reset. The Day-12 single-entity 'round-trip-closed' claim is now honest WITHOUT the single-entity asterisk. Enforced by `internal/database/l0_split_track13_test.go`.
- [ADR-0017: The Query Resolver — the READ half of the durability tier (/v1/query over Resolver.AsOf, Day 12)](0017_query_resolver_read_half_track12.md) — **ACCEPTED (Day 12, 2026-08-02)**: the round-trip-closed durability tier. Closes `[OPEN-P0-read]` — the mirror of the Day-11 WRITE half (ADR-0016). `pkg/mesh/control.go` gains a `/v1/query` route + a `SetResolver` seam (chosen over a `NewControlServer` arg — keeps the 4-arg constructor stable for the mesh tests, mirrors Day-11 `SetSnapshotter`); `cmd/sovereign-node` constructs `database.NewResolver(lfs, lfs, queryAlloc, "local", DefaultResolverConfig())` over the SAME `*LocalFS` `--lsm-root` binds, gated on `enableIndex`, and wires `SetResolver`. Law II byte-identity VERIFIED + empirically confirmed: WRITE dir `hex(sha256(eid)[:8])` == READ dir `hex(sha256(eid)[:8])`, WRITE col0 `sha256(eid)[:16]` == READ col0 filter — 50 checkpoints of `"alpha"` → 50 files at `l0/8ed3f6ad685b959e/` = `hex(sha256("alpha")[:8])`. The 503-honest-disabled contract (Day-8.5 precedent): nil-resolver `/v1/query` → 503 `{"error":"query-tier disabled (no --lsm-root)"}`, NOT a silent 404. The NO-RECOMPUTE digest law (Law V): `PayloadDigestHex` echoed verbatim, NO `payload` field (the index has a SENTRY body — a payload field is the G06.e fabrication; `TestQuery_RoundTrip` asserts the absence). Teeth T1–T4 + T-WITNESS in `pkg/mesh/query_test.go` red-then-green over a REAL `*LocalFS` (the E2E that was missing — the `.AsOf` surface was unit-isolated to `internal/database` with a `mockS3Store`): T1 write→checkpoint→query (SystemTime+digest verbatim); T2 crash→bounded-recover→query-the-history (`witness.Bounded`, @T2 queryable, T1 history retained, 2 files); T3 `-race` concurrent writer+checker+queryer (no race, no torn-read FP); T4 503-not-404; T-WITNESS N files after N checkpoints (asserted == N, debt DISCLOSED not fixed). Gates G12 PASS (build, vet [35 pre-existing unsafe notes in FROZEN `pkg/sync`+`internal/chaos`, zero in the Day-12 packages], gofmt, `-race` mesh+database+durability, `TestHotPathZeroAllocations` 0 allocs/op, internal/chaos). The track36 scope-tooth FAILS pre-commit (flags `pkg/mesh/control.go` outside the 3.6 set — the documented transient conflict; PASSES post-commit when working tree == HEAD; tooth NOT edited). HONEST SCOPE (§6): AsOf `O(L0-files)` per query (NOT constant-time); L0 unbounded growth disclosed NOT fixed (the compactor NOT wired — "Do not wire the dead compactor"; the epoch-tombstone compactor is DEAD-TO-DEAD, NO production DELETE operator); multi-entity flush directory-keying residual (a flush file is keyed by the smallest-hash entity — only that entity is retrievable by its own prefix); the `MaxValidTime.UnixNano()` overflow LATENT landmine (disclosed, out of READ-only scope; no prod caller hits it). `internal/database` UNREWRITTEN (interface conformance only); WRITE path untouched; hot path unchanged. §5 verdict STAYS CONDITIONAL-GO (Day 12 is NOT E1/E2/E3/E5; it ADVANCES the claim from "durability tier half-closed" to "round-trip-closed"). Enforced by `pkg/mesh/query_test.go`.
- [ADR-0026: The OTel-INIT Fork — Dedup `QueryL0ListCapped` in `init()`, Fill `rebuildCounters()`, Arm `telemetry.Init` at Boot (Day 21)](0026_otel_init_dedup_query_list_capped_track21.md) — **ACCEPTED (Day 21, 2026-08-08)**: closes the named OPEN-P1 residual ADR-0023 §6 carry-forward named — the OTel-INIT layer. Dedups the `init()` `QueryL0ListCapped` construction (2 → 1), FILLS `rebuildCounters()` (0 → 1, the load-bearing omission close — without it `telemetry.Init(realMeter)` reassigns `QueryL0ListCapped` to nil → the use-site guard `internal/database/query.go:253` silently stops counting the cap-hit disclosure counter), and arms `telemetry.Init(realMeter)` at boot in `cmd/sovereign-node` with a real `go.opentelemetry.io/otel/sdk/metric` `MeterProvider` (gated `--otel`; deps in `go.sum` v1.43.0 — `go.mod` tidied to promote `otel/sdk` + `otel/sdk/metric` from indirect to direct, the §0.b premise-audit #1; NO new VERSION added) with a `PeriodicReader` over a `logOutputExporter` (a SEPARATE destination from the bridge's prometheus Registry — the §0.d two-exporter separation refreshed; NOT a double-count). The `T_gc` auto-inference P1 (ADR-0020 §6a) is UNBLOCKED, NOT implemented (the operator still sets `T_gc` per the Day-20 contract). Premise-audit findings: (M1) the §1.1 "invocation count drops by 1 net" was double-counted (-1 init dedup + +1 rebuild fill = 0 net); (M2) §0.c "ZERO new module deps / DO NOT touch go.mod" was FALSE — `otel/sdk`+`sdk/metric` were in `go.sum` but NOT the `go.mod` direct `require` block, so `go build` needed `go mod tidy`; (M3) the schema-URL landmine (the SDK's `Default()` uses v1.40.0; the v1.4.0-era URL is stale → `resource.Merge` `ErrSchemaURLConflict` — caught by the first T-OTEL-EXPORT run + fixed). The dictated-flip (§0.e): the Day-18 T6 prose "telemetry.Init has ZERO production callers" REFRESHED to the gated `TestTrack21_T_Armed` contract (EXACTLY ONE production caller under `cmd/sovereign-node/`; the bridge does NOT call `Init`). 6 NEW teeth GATED+GREEN (T-DEDUP-INIT, T-DEDUP-FILL, T-SSoT-UNCHANGED, T-OMISSION-FIRED, T-OTEL-EXPORT in `internal/telemetry/day21_track21_test.go`; T-ARMED in `pkg/metrics/track21_t_armed_test.go`); the Init-driving teeth run in a subprocess (the `Init` once-per-process guard). §III GATE green: build/vet/gofmt clean; fieldalignment adds NO new findings (telemetry.go:111 + main.go:84/793 are pre-existing baseline stash-confirmable); 5-FILE FROZEN md5 byte-identical (835350a8/ed9132a2/47d2796a/590af228/b1beba1e — the FOURTH clean-chain fork, NO re-pin; ZERO FROZEN files touched); `TestGate_UntouchedFrozenAndOutOfScope` GREEN pre-AND-post; `-race` clean across `internal/telemetry`+`pkg/metrics` (the OTel periodic-reader `lastReported` path is NOT a race). §5 STAYS CONDITIONAL-GO. Enforced by `internal/telemetry/day21_track21_test.go` + `pkg/metrics/track21_t_armed_test.go`.
- [ADR-0027: The T_gc Auto-Inference Fork — Track the Observed Live-Query txTime Frontier, Feed `max(operatorFloor, observedFrontier - backoff)` into the Unchanged DominancePrune (Day 22)](0027_t_gc_auto_inference_track22.md) — **ACCEPTED (Day 22, 2026-08-08)**: closes the OPEN-P1 (a) the Day 19→20→20.1→21 chain named (ADR-0020 §6a — the `T_gc` auto-inference). The DominancePrune `(C3)` floor (`CompactionConfig.PruningHorizonInt64Ns`) was a STATIC operator knob the engine never advanced — the operator had to GUESS the live-query txTime frontier. Day 22 makes the engine TRACK the frontier itself (queried at `AsOf`-entry `query.go:237`, the `txTimeNs := transactionTime.UnixNano()` line — VERIFIED) and feed `effective = max(operatorFloor, observedFrontier - backoff)` back into the UNCHANGED `DominancePrune` call (`l1_compactor.go:710` — VERIFIED EXACT, the load-bearing seam; the `DominancePrune` function body is UNCHANGED — Day 22 changes the horizon SOURCE, not the prune). The inferrer FLOORS the operator knob (does NOT replace it): the operator-set `PruningHorizonInt64Ns` is the HARD floor backstop (the inferrer NEVER goes below it — the §0.4(ii) txTime-gap silent-data-loss class Day 15 closed stays closed); the inferrer ADVANCES the effective horizon ABOVE it as the workload's txTime frontier moves. The monotone clamp is LOUD (a retreat is refused + counted + logged — the §0.4(ii) class stays closed; Day 22 never silently lowers the horizon). 3 NEW counters (`QueryTxTimeHighWaterMark` modeGauge, `PruningHorizonEffective` modeGauge, `PruningHorizonRetreatRefused` modeCounter) in `init()` + `rebuildCounters()` + `allCounters()` (the Day-21 fill discipline — the §0.f construction-vs-distinct trap disarmed; a counter missing `rebuildCounters` silently drops to nil under `--otel`); `allCounters()` grows 12→15 (honest growth). The bridge auto-surfaces the 3 new counters WITHOUT a bridge edit (the §0.f SSoT-grows-auto property — byte-verified UNCHANGED at HEAD; the 2 gauges report the last-Set values on `/metrics`, the §6.e claim bridge-side). Premise-audit findings (the §0.b discipline, FIFTH dictated-correction): (M1) §2.b 'a SECOND CALL: operatorFloor=6000' was a misread — the operator floor is construction-time + immutable, so two operatorFloor values are TWO compactors; (M2 THE LOAD-BEARING ONE) §2.c `T-INFER-EQUIV` as DICTATED cannot catch an inferrer off-by-one — `DominancePrune` is PURE + UNCHANGED, fed the SAME horizon on both sides → byte-identity holds BY CONSTRUCTION → an off-by-one is INVISIBLE; the tooth is authored HONESTLY (proves the prune is byte-identical UNDER THE NEW HORIZON SOURCE; the off-by-one burden carried by `T-INFER-FLOOR` + `T-INFER-REAL-LocalFS`); (M3) the modeGauge scrape scientific-notation trap — prometheus renders ≥1e6 as `7.777777e+06`; `scrapeCounterValue` stops at the `e` → the tooth uses gauge values <1e6 (the §6.e claim asserted identically at <1e6); (M4) the count-growth class the prompt's §1.c named for the Day-21 `TestTrack21_T_SSoTUnchanged` tooth (12→15) applied to the Day-18 `TestTrack18_InPackage_T2_SSoTNoDup` tooth too — it hardcoded `wantDistinct=12` + `gauges!=1` (exactly 1 gauge); Day 22 adds 2 gauges → 1→3 + counters 11→12; BOTH the Day-21 + Day-18 teeth re-pinned 12→15 (+ the Day-18 gauge/counter split 1+11→3+12); (M5) the §2.h NoOp control PROVES the inferrer's advance is load-bearing — the operator floor ALONE (100) would REFUSE the drop (the dominator 250>100 → `RowsPruned=0`); the inferrer's advance to 1600 (`max(100, 2000-400)`) ADMITS it (`RowsPruned=1`); the no-op control leaves `RowsPruned=0` (the divergence). 8 NEW teeth GATED+GREEN (10 sub-teeth across 3 NEW test files — the import-cycle-forced split, the Day-19 precedent): `T-INFER-ASOF-OBSERVES` (+ConcurrentRace, `internal/database` — 3200 concurrent `AsOf`, 0 races under `-race`), `T-INFER-MONOTONE` (+RedControl_NoClamp), `T-INFER-FLOOR` (+RedControl_IgnoreOperatorFloor), `T-INFER-EQUIV` (10k fuzz, seed=22), `T-INFER-ALLOC` (MEASURED delta=0 — the seam is `LoadInt64`+`CompareAndSwapInt64` on a struct int64, ZERO alloc; the 2 allocs/op is the read-path residual NOT the write-path gate), `T-INFER-TELEMETRY-SSoT` (+NewCountersNonNilPostInit_Subproc, `internal/telemetry` — the 3 counters NON-nil post-Init + observed by the OTel `ManualReader` `Collect`), `T-INFER-RETREAT-COUNTER-LOUD` (+GREEN control), `T-INFER-REAL-LocalFS` (+NoOpControl, `pkg/durability` — the end-to-end route: write R+R' → `AsOf(txTime=2000)` advances the frontier → inferrer `max(100,2000-400)=1600` → compact → R DROPPED (`RowsPruned=1`) → LIVE `AsOf` resolves to R' — NO silent data loss). Plus the bridge-enumeration sister tooth `TestTrack22_T_BridgeAutoSurfacesNewCounters` (`pkg/metrics` — the 3 new counters auto-surface on `/metrics` WITHOUT a bridge edit; the 2 gauges report the last-Set values; bridge UNCHANGED at HEAD). The 2 re-pinned teeth: `TestTrack21_T_SSoTUnchanged` (12→15) + `TestTrack18_InPackage_T2_SSoTNoDup` (12→15, gauges 1→3, counters 11→12). `TestTrack21_T_Armed` GREEN (the inferrer reads counters, does NOT call `Init` — the §0.d two-exporter separation holds). §III GATE green: build/vet/gofmt clean; fieldalignment adds NO new findings (the 2 `l1_compactor.go` findings are the PRE-EXISTING `mergedRowT` 144→32 + `CompactionResult` 56→40, line-shifted 213/226→382/395 by the added code; the NEW structs `Resolver`+int64 / `CompactionConfig`+`PruneBackoffInt64Ns` / `horizonInferrer` / `L1Compactor`+`inferrer` are NOT flagged); 5-FILE FROZEN md5 byte-identical (835350a8/ed9132a2/47d2796a/590af228/b1beba1e — the FIFTH clean-chain fork after Day-18/19/20/21; NO re-pin; ZERO FROZEN files touched); `TestGate_UntouchedFrozenAndOutOfScope` + `TestGate_FrozenMD5` GREEN pre-AND-post; `TestHotPathZeroAllocations` GREEN (Day 22 touches the READ path + the compaction scheduler, NOT the write path); `-race` clean per-package across `internal/database`+`internal/telemetry`+`pkg/metrics`+`pkg/durability` (the 4-core-box per-package pattern; the 10k fuzz run non-race). §5 STAYS CONDITIONAL-GO (Day 22 is NOT an E1/E2/E3/E5 verdict-blocker; it does NOT touch the single-writer root-equality qualifier — the MANDATE-2 the FROZEN `crdt.go` blocks). Enforced by `internal/database/day22_track22_test.go` + `internal/telemetry/day22_track22_test.go` + `pkg/durability/day22_track22_test.go` + `pkg/metrics/day22_track22_test.go`.
- [ADR-0028: SkipListArena.Seek — the Logarithmic Window Lower-Bound Iterator (Day 23, Track 23)](0028_skiplist_seek_window_lower_bound_track23.md) — **ACCEPTED (Day 23, 2026-08-08)**: closes the FIRST claw of the OPEN-P1 `AGENTS.md:60` — `[OPEN-P1] Range scan via internal/database/skiplist_arena.go`. `SkipListArena.Seek(target []byte) *SkipListIterator` (`skiplist_arena.go:355`) is the `Put` descent EXPOSED as a read primitive: top→bottom, advance while `bytes.Compare(nodeKey(nxt), target) < 0`, break on `>= target` OR the `0xFFFFFFFF` end-of-level sentinel; return `nodeNext(prev[0], 0)` = the first node whose key >= target (the LOWER-BOUND, NOT a point lookup; `0xFFFFFFFF` sentinel if target > all keys). Byte-identical to `Put`'s Phase-1 descent (`skiplist_arena.go:236-250`) with the prev[i]/next[i] stores elided. Plus `NewSeekIterator(target)` (`:411`) — the one-liner symmetry with `NewIterator` (full-scan vs lower-bound-scan by constructor). Plus the FIXED stale `Key()` docstring (:433 — `[16B hash][8B sysTime][8B validTime][8B assertTime] = 40 bytes`, Override 8.4; was the stale 8B/32B Day-9-era comment). THE LOAD-BEARING FINDING (the §0.d honesty gate FIRING on the bytes — the day-17/18/20/22 premise-correction class): the prompt's §0.d reasoned "the bottom-up CAS-splice guarantees a concurrent Seek returns a valid lower-bound" — T-SEEK-CONCURRENT CAUGHT THIS FALSE pre-fix (~1-2% of seeks returned a node whose key < target under a 100k-key concurrent Put workload; the §0.c sentinel class fired worst: target > all keys returned the LAST node instead of the `0xFFFFFFFF` sentinel). Root cause: `Put`'s search is correct under concurrent inserts ONLY because its Phase-2 CAS-splice FAILS + re-searches when a predecessor's next moved (`skiplist_arena.go:273-287`) — the CAS is the self-correction; a READ-ONLY descent has NO CAS, so the multi-level descent can observe a transiently-inconsistent snapshot (a high-level predecessor's level-0 successor whose key is < target at the read moment — the bottom-up splice is settling). The level-0-only linear scan (`NewIterator` walk) NEVER violated — PROVEN (the differential test ran BOTH concurrently: linear viol=0, Seek viol>0; ONLY the multi-level descent diverged; the level-0 chain is the STABLE total order, the upper levels go transient). THE FIX (honest, lock-free — `skiplist_arena.go:378-383`): after the descent, VERIFY the level-0 result (sentinel OR `nodeKey(cur) >= target`); on the rare transient, fall back to a FRESH level-0 linear walk (`seekLowerBoundLinear` `:395` — the stable total order, always a valid lower-bound). Lock-free (no mutex); always a valid lower-bound under concurrency; O(maxHeight) quiescent (the guard is a no-op when the descent is consistent — the production precondition: Seek's only consumer today is `l0_flusher.go:198` `FlushArenaToIPC` over a FROZEN arena; the intended Range wiring reads frozen/durable data); O(N) only on the rare concurrent transient (~1-2% of seeks under a heavy concurrent writer — the disclosed cost of correctness). Premise-audit (the §0.b discipline, SIXTH dictated-correction): (M1) §0.d "nodeNext is a relaxed read → torn next-pointer → use atomic.LoadUint32" was FALSE on the bytes — `nodeNext` (`:108-111`) ALREADY uses `atomic.LoadUint32`; the relaxed-memory hazard that fires is STRUCTURAL (no CAS self-correction), NOT a torn next-pointer; (M2) §2.c "random16bytes hash for the fuzz targets" would land ~99.99% OUTSIDE the single entity (prob 2^-128) → the fuzz SPLITS targets EVEN=fixed-entity-hash (within-entity, the load-bearing case) / ODD=random-hash (cross-entity); (M3 THE LOAD-BEARING ONE) the bottom-up-splice-guarantee was FALSE on the bytes (above); (M4) §2.a "seed the global rand" is NOT POSSIBLE in `math/rand/v2` (v1 `rand.Seed` removed) — the skiplist heights are non-deterministic; harmless (T-SEEK-EQUIV compares Seek vs linear-scan for the ACTUAL skiplist each run); the fuzz TARGETS are deterministic (`rand.New(rand.NewPCG(23,0))`); (M5) §0.c "sentinel 0" was a name/value conflation — `offsetNil=0` is the CONST NAME but its VALUE is `0xFFFFFFFF` (offset 0 is the head); the teeth + code use `0xFFFFFFFF` throughout. 7 NEW teeth GATED+GREEN + 2 benches: `T-SEEK-LOWERBOUND` (N=4096, every key its own lower-bound + the just-before lower-bound + `NewSeekIterator==Seek`), `T-SEEK-BEFORE-FIRST` (target < first key → the FIRST node, byte-identity with `NewIterator`), `T-SEEK-EQUIV` (LOAD-BEARING — 10k fuzz seed=23, `Seek` == the O(N) linear-scan lower-bound for ALL targets, 10000/10000 byte-identical), `T-SEEK-END` (target past last key → `0xFFFFFFFF` sentinel, NOT the last node), `T-SEEK-EMPTY` (fresh skiplist → exhausted iterator, no panic), `T-SEEK-ALLOC` (HONEST — MEASURED 1 alloc/run, the `*SkipListIterator` struct; per-descent-step 0; target on stack — Law V "1 alloc", NOT "zero-alloc"), `T-SEEK-CONCURRENT` (the gate that earned the headline — under `-race`, M=8 readers * 1000 Seeks + N=100000 concurrent Puts, 0 races + 0 lower-bound violations post-fix, every Seek a valid lower-bound re-checked post-quiescent). Benches `BenchmarkTrack23_SeekLogN` + `BenchmarkTrack23_LinearScanLogN` (N=131072): MEASURED Seek ~714-743 ns/op 16B/op 1 allocs/op vs LinearScan ~429,080-429,462 ns/op 0B/op 0 allocs/op → **~600× acceleration** (the honest headline — Law V, NOT the "O(log N)" adjective; §6.c: the descent is O(maxHeight) CAPPED at 11, NOT unbounded O(log N); for N>>2048 the descent saturates at 11). §III GATE green: build/vet(`internal/database`)/gofmt clean (the `go vet ./...` `unsafe.Pointer` warnings are ALL in `pkg/sync`, PRE-EXISTING at HEAD, stash-confirmable; Day 23 added ZERO new vet findings); fieldalignment adds NO new finding (the `skiplist_arena.go:40` finding is the PRE-EXISTING `SkipListArena` struct, byte-identical to HEAD; edits at :293+ added zero new findings); 5-FILE FROZEN md5 byte-identical (835350a8/ed9132a2/47d2796a/590af228/b1beba1e — the SIXTH clean-chain fork after Day-18/19/20/21/22; NO re-pin; ZERO FROZEN files touched); `TestGate_UntouchedFrozenAndOutOfScope` + `TestGate_FrozenMD5` GREEN pre-AND-post; `TestHotPathZeroAllocations` GREEN in `pkg/sync` (Day 23 touches the READ path in `internal/database`, NOT the write path); `-race` clean per-package `internal/database`. §5 STAYS CONDITIONAL-GO (Day 23 is NOT an E1/E2/E3/E5 verdict-blocker; it does NOT touch the single-writer root-equality qualifier — the MANDATE-2 the FROZEN `crdt.go` blocks). The WIRING of Seek into `scanWindowRecordBatch` (the O(L0-files) AsOf residual ADR-0017 §6 named — the SECOND claw of the OPEN-P1 line) is the NEXT fork, disclosed NOT closed. Enforced by `internal/database/day23_track23_test.go`.
- [ADR-0029: The Filename-Bounded Download Skip — the Transitively-Safe Elimination on the FILE Channel (Day 24)](0029_filename_bounded_download_skip_track24.md) — **ACCEPTED (Day 24, 2026-08-09)**: closes the FIRST channel of the durable AsOf/Range unbounded-download residual ADR-0017 §6 named. The durable read path (AsOf+Range) parses each L0/L1 file's filename-encoded `FirstSysTimeNs` (the file's MIN sysTime, written by the flush since Day-13) + SKIPS the Download when `firstSys > txTime` (STRICT `>`; a row AT `sysTime==txTime` passes Filter2 with `<=` so `firstSys==txTime` → DO NOT skip). Transitively-safe elimination (a TAUTOLOGY): `file.min>T ⟹ every row fails Filter2 ⟹ ZERO qualifying rows ⟹ skip preserves the answer byte-identically` (Law II) AND cuts the download count. `parseFirstSysFromKey` zero-alloc (`LastIndexByte`+`ParseInt` base 10) + FAILSAFE (parse anomaly → `ok=false` → NO skip → full download; a corrupt filename NEVER silently dropped). `ResolverConfig.EnableFirstSysSkip` defaults TRUE (opt-OUT; Day 24 INVERTS Day-19's opt-IN). Premise-audit (SEVENTH dictated-correction): (M1 load-bearing) the queue's "wire Seek into `scanWindowRecordBatch`" was FALSE on the bytes — `scanWindowRecordBatch` takes `arrow.Record` (`query.go:1013`), NOT a SkipList; the Resolver struct (`query.go:104`) holds NO live MemTable/SkipList field; the durable read path reads Arrow files ONLY — wiring Seek into the durable Arrow read is a NO-OP; the genuine residual is the DOWNLOAD count, NOT the row scan; the honest acceleration target is the filename (which ALREADY carries the bound), NOT the SkipList; (M2) SkipList key order `hash|sysTime|validStart|assertTime` (validStart co-sorted NOT independent — a Seek can bound AT MOST the sysTime ceiling, NOT the `[vLo,vHi)` validTime window); (M3) Seek stays DORMANT (NOT ROI to wire; the genuine first consumer is the live cross-entity tail mega-fork, NOT the durable Arrow wiring); (M4) count-growth 15→16 re-pinned Day-18/21/22 SSoT teeth (`day18` wantCounters 12→13, gauges UNCHANGED at 3 — the SAME class Day-22 M4 hit). `QueryDownloadSkippedFirstSys` counter (modeCounter) in all 4 sites (the Day-21 fill); the bridge auto-surfaces the 16th series WITHOUT a bridge edit (the §0.f SSoT-grows-auto property — bridge byte-UNCHANGED, the 7th clean-chain fork, NO bridge re-pin since Day-13). 8+4+2+1 teeth GATED+GREEN (in-package + REAL-`*LocalFS` + SSoT + bridge-sister); the EQUIV fuzz (`N=64×2000` seed=24) PROVED load-bearing via BUG-INJECT (`>=`→divergences at every `firstSys==txNs` → RED); the DOWNLOAD-COUNT tooth PROVED load-bearing via RED-NEUTER (downloads 4→2, skips 0→2). §III GATE green: build/vet(`internal/database`+`internal/telemetry`+`pkg/durability`+`pkg/metrics`)/gofmt clean (the `go vet ./...` `unsafe.Pointer` warnings are ALL in `pkg/sync`, PRE-EXISTING — vet 35==35); fieldalignment adds ZERO new debt (the `loadSupersededL0Keys` signature change adds an `int64` PARAM, NOT a struct field; `ResolverConfig` UNCHANGED); 5-FILE FROZEN md5 byte-identical (`835350a8`/`ed9132a2`/`47d2796a`/`590af228`/`b1beba1e`); verifier-side byte-UNCHANGED (`3c1b4a8f`/`2ed28034`/`22c36f61`/`8fcc149b` — l0_flusher/l1_compactor/skiplist_arena/telemetry_bridge); `TestGate_FrozenMD5`+`TestGate_UntouchedFrozenAndOutOfScope` GREEN pre-AND-post; `TestHotPathZeroAllocations` GREEN (READ path, NOT write); `-race` clean per-package (the 4-core box constraint). §5 STAYS CONDITIONAL-GO. Enforced by `internal/database/day24_track24_test.go` + `pkg/durability/day24_track24_test.go` + `internal/telemetry/day24_track24_test.go` + `pkg/metrics/day24_track24_test.go`.
- [ADR-0030: The Manifest-Channel Download Skip — the Transitively-Safe Elimination on the SECOND Download Channel (Day 25)](0030_manifest_channel_download_skip_track25.md) — **ACCEPTED (Day 25, 2026-08-09)**: closes the SECOND channel of the durable AsOf/Range unbounded-download residual ADR-0017 §6 named. Day 24 (ADR-0029) closed the FILE channel; Day 25 closes the MANIFEST channel. `loadSupersededL0Keys` downloads + `ParseManifest`-decodes EVERY compaction manifest per query (one per compaction job, `compaction/{hex8}/{firstSys}.manifest`) to mark L0 keys superseded before the tail cap. The manifest's filename-encoded `firstSys` is the L1's MIN sysTime — the SAME field Day-24 skips on the file channel (`manifestKeyFor`+`l1KeyFor` share `firstSysT`, `l1_compactor.go:736-737/793-794` — byte-identical). When `firstSys > txTime` (STRICT `>`, the Day-24 boundary), the L1 the manifest points at is file-skipped (Day-24 scan loop) AND every L0 the manifest lists is file-skipped (its `firstSys >= manifest.firstSys > txTime`) → skipping the manifest DOWNLOAD preserves the superseded set w.r.t. the query's VISIBLE rows byte-identically (Law II) AND cuts a manifest Download (+ the `ParseManifest` `strings.Split` alloc). The skip reuses `parseFirstSysFromKey` EXACTLY (the parser was NEVER an `.arrow`-specific reader — M1: the grammar is SHAPE-based, NOT tier-based; the `.manifest` tail parses identically; the prior Day-24 docstring denying it was a DOCUMENTATION ERROR, corrected). Premise-audit (EIGHTH dictated-correction): (M1 load-bearing) `parseFirstSysFromKey` parses `.manifest` keys (the §0 probe byte-verified `ok=true`; Day-25 USES the property + corrects the docstring, does NOT add a `.manifest` special case — the fork would be a no-op if it returned `ok=false` for `.manifest`); (M2 load-bearing) the prompt's RED-control-2 "a `>=` bug causes the EQUIV (dominant) to diverge at every `txTime==manifest.firstSys`" was FALSE for the DOMINANT — the manifest skip affects the SUPERSEDED map (which L0s leave `tailKeys`), NOT the L1 (scanned via `l1Keys` REGARDLESS of the manifest skip); the L1 is the compaction's Preserve-All merge (`DefaultCompactionConfig` `EnableDominancePruning=false`) → it carries the SAME rows → the dominant stays byte-identical under `>=` (the `>=` bug is a PERF regression, NOT a Law-II break); the honest `>=` catcher is the SUPERSEDED-MAP SUBSET assertion (manifest-skip's removed-superseded-keys ⊆ the Day-24 file-skip's removed scan-keys), NOT a fabricated dominant divergence (`T-MANIFEST-EQUIV-RED-OPPOSITE` catches the `<=` bug via the subset assertion; `T-MANIFEST-EQUIV-RED-NOOP` catches the no-op bug via the download-count counter-fires-`>=1` assertion); (M3) the manifest skip reuses `EnableFirstSysSkip` (NO new flag — the manifest+file skips are the SAME elimination on two channels of the SAME query; a second flag is a non-uniformity with NO production rationale); (M4) count-growth 16→17 re-pinned Day-18/21/22/24 SSoT teeth (the SAME class Day-22 M4 + Day-24 hit again — the count-assertion teeth hardcode the count; the SSoT grows, the assertion teeth follow); (M5) the `maxSys` upper-bound sidecar DISCARDED as ORTHOGONAL (bounds the OPPOSITE end `file.max<txTime`; NOT transitively-safe — a heuristic not a tautology; a SEPARATE opt-in fork, NOT this one; Day-25 closes the LOWER bound on the manifest channel ONLY). `QueryManifestSkippedFirstSys` counter (modeCounter) in all 4 sites (the Day-21 fill discipline held for the 17th); the bridge auto-surfaces the 17th series WITHOUT a bridge edit (the §0.f SSoT-grows-auto property — bridge byte-UNCHANGED, the EIGHTH clean-chain fork, NO bridge re-pin since Day-13). 15 teeth GATED+GREEN across 4 packages (10 in-package incl 2 RED controls + 3 REAL-`*LocalFS` route + 2 SSoT + 1 bridge-sister) + 4 re-pinned count teeth; the EQUIV fuzz (`N=32×2000` seed=25, byte-identical dominant, 46/2000 manifest-skip fires, 0 divergences); the DOWNLOAD-COUNT (in-package + REAL-`*LocalFS`) PROVED the cut (manifestDLs 1→0, l1DLs 1→0; the L0s superseded under BOTH paths — the Day-14 supersession contract); RANGE-WINDOW-ORTHOGONAL (the manifest skip bounds sysTime Filter2, Range bounds validTime Filter3 — ORTHOGONAL). §III GATE green: build/vet(`internal/database`+`internal/telemetry`+`pkg/durability`+`pkg/metrics`)/gofmt clean (the `go vet ./...` `unsafe.Pointer` warnings are ALL in `pkg/sync`, PRE-EXISTING — vet 35==35); fieldalignment adds ZERO new debt (database 11==11, telemetry 1==1 — UNCHANGED from Day-24/HEAD; the `loadSupersededL0Keys` signature change adds an `int64` PARAM, NOT a struct field; `ResolverConfig` UNCHANGED); 5-FILE FROZEN md5 byte-identical (`835350a8`/`ed9132a2`/`47d2796a`/`590af228`/`b1beba1e`); verifier-side byte-UNCHANGED (`3c1b4a8f`/`2ed28034`/`22c36f61`/`8fcc149b` — l0_flusher/l1_compactor/skiplist_arena/telemetry_bridge; Day-25 READ-ONLY-verifies the manifest producer's grammar); `TestGate_FrozenMD5`+`TestGate_UntouchedFrozenAndOutOfScope` GREEN pre-AND-post; `TestHotPathZeroAllocations` GREEN (READ path + telemetry, NOT write); `-race` clean per-package (the 4-core box constraint; `internal/database` 351.255s HONEST — NOT Day-24's fabricated "1.0s"; `pkg/durability` 1.104s; `internal/telemetry` 8.180s; `pkg/metrics` 1.162s). §5 STAYS CONDITIONAL-GO (Day 25 is NOT an E1/E2/E3/E5 verdict-blocker; it does NOT touch the single-writer root-equality qualifier — the MANDATE-2 the FROZEN `crdt.go` blocks). ZERO FROZEN touched (the EIGHTH clean-chain fork after Day-18/19/20/21/22/23.1/24). Enforced by `internal/database/day25_track25_test.go` + `pkg/durability/day25_track25_test.go` + `internal/telemetry/day25_track25_test.go` + `pkg/metrics/day25_track25_test.go`.
- [ADR-0031: The Zero-Alloc-Line Streaming ParseManifest — the ADR-0030 §6.a Carry-Forward CLOSED (Day 26)](0031_zero_alloc_streaming_parsemanifest_track26.md) — **ACCEPTED (Day 26, 2026-08-09)**: closes the ADR-0030 §6.a residual the Day-25 fork OPENED + NAMED (the zero-alloc `ParseManifest` streaming line reader). `ParseManifest` (`l1_compactor.go`) used `strings.Split(string(body),"\n")` — an intermediate `[]string` of N+1 entries IN ADDITION to the `l0Keys` slice; the 3 callers used `io.ReadAll` (a 512B-start doubling). Day 26 swaps BOTH (byte-identical): `ParseManifest` body → a single-pass `strings.IndexByte` scan over `string(body)` (substring-append `l0Keys` — 0 per-line copy, aliasing the ONE `string(body)` copy; the `first` flag mirrors the old `i==0` unconditional-set+`continue` for the malformed-line-0 edge); the 3 caller sites' `io.ReadAll(rc)` → a new `readManifestBody` (single-grow 4096B-start buffer, reads directly into the growable slice — no separate scratch; covers the observed max 2990B in ONE alloc). The `ParseManifest` signature + the `buildManifest`/`manifestKeyFor` writers are byte-UNCHANGED. Premise-audit (NINTH dictated-correction): (M1 LOAD-BEARING — the §0.b honesty gate, MEASURED before code) the prompt's EDIT-1 candidate (`bytes.IndexByte` + `string(line)` per L0) was REFUTED — the OLD `strings.Split` substrings ALIAS the one `string(body)` copy (0 per-line alloc — a `[]byte`→`string` conversion is NOT free; strings are immutable so `string([]byte)` COPIES); the candidate's per-line copy is ADDITIVE — MEASURED (`testing.AllocsPerRun`, `T-STREAM-ALLOC`) 3-29× WORSE (N=1 tie 3==3; N=16 20 vs 5; N=64 70 vs 7; N=256 264 vs 9); the §0.b honesty gate KILLED it, NOT shipped (the `T-STREAM-ALLOC` tooth's `require.Greater(refuted,old)` for N>1 makes the rejection EXECUTABLE); the HONEST replacement keeps the ONE `string(body)` copy (irreducible — `l0Keys` are `map[string]struct{}` keys, need stable strings not sub-slices of a buffer the caller frees) + drops the Split slice; (M2) `strings.TrimSpace`/`bytes.TrimSpace` return sub-string/sub-slice (NOT the alloc source); (M3) the malformed-body behavior (the `first`-line unconditional-set + the stray-line defense-in-depth) preserved EXACTLY — the reaper's `l1Key==""` guard depends on it; (M4 count-growth) Day 26 adds NO counter — the SSoT STAYS at 17 (the FIRST pure-refactor fork since Day-22's count-growth class; the Track18/21/22/24/25 `wantDistinct` teeth UNCHANGED, NO re-pin). MEASURED E2E win (`T-STREAM-ALLOC`): N=64 (the per-query `loadSupersededL0Keys` caller) 15→9 allocs/run (−6, −40%); N=1 6→5; N=16 10→7; N=256 21→13. 6 teeth GATED+GREEN: 5 in `internal/database` (`T-STREAM-BYTE-IDENTITY` LOAD-BEARING — 2000 fuzzed manifests seed=26, `ParseManifest` + the REFUTED candidate byte-identical to the reference on OUTPUT, 0 divergences; `T-STREAM-ALLOC` MEASURED+HONEST — `new<old` on BOTH axes at every N, the REFUTED candidate proven WORSE for N>1, NOT a "0 allocs" lie; `T-STREAM-RED-CONTROL` — the malformed `l0/`-line-0 edge, the `first`+`continue` load-bearing — a prefix-gated RED variant DIVERGES on `l1Key` AND `l0Keys`; `T-STREAM-READ-BODY` — `readManifestBody` byte-identical to `io.ReadAll` across empty/tiny/fits/grow + a fragmenting reader; `T-STREAM-SSOT-UNCHANGED` — `Counters()==17` DISTINCT, NO re-pin) + 1 composition tooth in `pkg/durability` (`T-STREAM-LOADSUPERSEDED-REALLocalFS` — a REAL `Compaction` over REAL `*LocalFS`, the manifest IS downloaded `manifestDLs==1` (the NEW parse ran, NOT the Day-25 skip), the 4 L0s superseded `l0DLs==0`, dominant `base+1000` `"row-1"` byte-identical to Day-25). Day-24/25 `T_*_FROZEN` teeth RE-PINNED `l1_compactor.go` `2ed280348`→`d0830b43c` (the reader changed; the writers `buildManifest`+`manifestKeyFor` byte-UNCHANGED via substring guards — finer-than-md5). §III GATE green: build/vet(`internal/database`)/gofmt clean; fieldalignment ZERO new debt (database 11==11, telemetry 1==1, metrics 0==0 — `ParseManifest`+`readManifestBody` are FUNCS not structs); 5-FILE FROZEN md5 byte-identical (`835350a8`/`ed9132a2`/`47d2796a`/`590af228`/`b1beba1e`); verifier-side byte-UNCHANGED EXCEPT `l1_compactor.go` re-pinned (l0_flusher/skiplist_arena/telemetry_bridge UNCHANGED); `TestGate_FrozenMD5`+`TestGate_UntouchedFrozenAndOutOfScope`+`TestGate_GearHonesty`+`TestBench_FrozenMD5` GREEN (the authoritative `pkg/receive` gate); `TestHotPathZeroAllocations` GREEN (READ path + reaper, NOT write); the Day-18..25 byte-identity suites GREEN (`stringScan` byte-identical to `strings.Split` over the full read path); `-race` clean per-package (the 4-core box constraint; the FULL `internal/database` -race hits the 4-core-box 600s+ wall-clock timeout on the PRE-EXISTING Day-20 `TestTrack20_T_Equiv_DifferentialFuzz`, NOT a Day-26 regression — that test has ZERO `ParseManifest` refs, committed `70e2105`, passes 25.8s no-race). §5 STAYS CONDITIONAL-GO (Day 26 is NOT an E1/E2/E3/E5 verdict-blocker; a pure-refactor READ-path + reaper fork, does NOT touch the single-writer root-equality qualifier — the MANDATE-2 the FROZEN `crdt.go` blocks). ZERO FROZEN touched (the NINTH clean-chain fork after Day-18/19/20/21/22/23.1/24/25). Enforced by `internal/database/day26_track26_test.go` + `pkg/durability/day26_track26_test.go` + the re-pinned `internal/database/day24_track24_test.go` + `internal/database/day25_track25_test.go`.
- [ADR-0032: The Read-Your-Writes Live δ-CRDT HAMT Seam — the LiveSource Interface + the AsOf/Range Live-Merge (Day 27)](0032_live_source_read_your_writes_track27.md) — **ACCEPTED (Day 27, 2026-08-10)**: closes the read-your-writes gap the Day-26 default-checkpoint-interval behavior EXPOSED (NOT a named ADR-0017 §6 residual — a NEW class). `Resolver.AsOf`/`.Range` read ONLY durable Arrow files; an entry a `POST /v1/insert` places in the live δ-CRDT HAMT (`engine.InsertLocal` CAS-spins the per-shard root, `crdt.go:983`) is INVISIBLE to `/v1/query` until the bridge's periodic `AppendCheckpoint` flushes `SnapshotToLSM`→L0 (`bridge.go:213`, default interval 1000 → every IMMEDIATE query 404'd — empirically confirmed Day-26; `--wal-checkpoint-interval=1` was REQUIRED for the durable read path to work at all). Day 27: a `LiveSource` interface (`internal/database/query.go:159`, owned by `internal/database` — it does NOT import `pkg/sync`, so the seam is an interface NOT a concrete engine; the Day-12 `SetResolver` precedent) whose `cmd/sovereign-node` `engineHAMTAdapter` (`main.go:1280`) wraps `engine.State().Get` + the EBR pin (the EXACT `SnapshotToLSM` concurrent-read guard, `snapshot.go:391-394`), wired at `main.go:419` (`queryResolver.SetLiveSource(&engineHAMTAdapter{engine: engine})`). The Resolver consults the live HAMT AFTER its durable Arrow scan + merges the live dominant under the SAME bitemporal dominance (Filter2 `sysTime<=txTime` + Filter3 validTime window + max-`SystemTime`-wins `>` not `>=`) — insert is visible to `/v1/query` IMMEDIATELY, before ANY checkpoint. OPT-IN: a nil `LiveSource` (the `DefaultResolverConfig` value — a research node / `--lsm-root` absent) keeps the durable-only behavior BYTE-IDENTICAL to Day-26 (`T-LIVE-OFF-IS-BYTE-IDENTICAL`); the `:442` AsOf + `:775` Range zero-durable early-exits are GUARDED on `r.liveSource==nil` (skip with a live source → fall through to the scan → the live merge at `:590`/`:890` seeds/appends). TENTH dictated-correction premise-audit: (M1 LOAD-BEARING) the prior draft's "freeze the live SkipList + read it directly via the Day-23 `Seek`" was REFUTED — `Seek` is DORMANT (ADR-0028, the streak-breaker deferred), the live MemTable is a per-shard CAS-spun HAMT NOT a frozen SkipList (a SkipList read would NOT see cross-shard dots), + `engine.State().Get` already meets the contract WITHOUT a freeze; FIX = the `LiveSource` interface + the `engineHAMTAdapter` (SkipList stays DORMANT, read-only streak stays alive); (M2) the seam is an interface NOT a `*crdt.Engine` (`internal/database` does NOT import `pkg/sync` — the import would close the `internal/` boundary); (M3 LOAD-BEARING) `live ⊇ durable` ALWAYS (the live HAMT is APPEND-ONLY, NEVER pruned; the durable Arrow index is built FROM it) → the `Range` merge DEDUPS live against durable by `(SystemTime, PayloadDigest)` (`liveRowKey`, `query.go:1721`) before append (a naive append would DUPLICATE every checkpointed row → a Law II break the prompt's M6 tautology assumed away — M6 reasons about AsOf's SINGLE dominant max-wins dedup-free, NOT `Range`'s MULTI-ROW slice); (M4) the Resolver defensively RE-CHECKS Filter2 (`query.go:610`, `SystemTime > txTimeNs` → drop) — zero cost on a correct adapter + a hard guarantee against a buggy/foreign one (a silent Law II break the trust-only design would have shipped); (M5 count-growth) 17→18 (`QueryLiveSourceReads`, `registry.go:144`, Prom `supremum.query.live_source_reads`) — the 18th distinct counter; the Day-18/21/22/24/25/26 `wantDistinct` teeth RE-PINNED 17→18 (the SAME class Day-22/24/25 M4 hit); the bridge auto-surfaces the 18th series ZERO edit (§0.f, bridge byte-UNCHANGED — the TENTH clean-chain fork, NO bridge re-pin since Day-13); (M6 the runtime-verify-caught bug) the first runtime pass 200'd the IMMEDIATE point query (AsOf `:442` guarded) but 404'd the IMMEDIATE range — the `Range` `:775` zero-durable early-exit was NOT guarded on `r.liveSource==nil`, so the live-merge block at `:890` never ran (`QueryLiveSourceReads` was 2 not 3); the `T-LIVE-RANGE` tooth used a NON-empty durable tier so MISSED it; the runtime verify over a REAL empty-durable engine CAUGHT it; FIX = the load-bearing symmetry (`:442` + `:775` BOTH guard on `r.liveSource==nil`); `T-LIVE-RANGE-EMPTY-DURABLE` (BUG-INJECT-proven) is the 9th tooth + the regression guard the bytes forced into existence. THREE §0.b refinements the prompt hand-waved (M-ASOF-EARLY-EXIT, M-RANGE-DEDUP, M-DEFENSIVE-FILTER2 — disclosed in ADR-0032 §3, NOT silently filled in). GATE: 12 teeth across 3 packages (8 in `internal/database` incl `T-LIVE-MERGE-EQUIV` LOAD-BEARING + `T-LIVE-RANGE-EMPTY-DURABLE` the runtime-caught guard + 2 RED/BUG-INJECT controls; 2 in `internal/telemetry` incl the SSoT 18 + the subprocess NON-NIL-post-init; 2 in `pkg/durability` — `T-LIVE-READ-YOUR-WRITES` the load-bearing runtime proof + `T-LIVE-EBR-RACE` the concurrent-read pin correctness). RUNTIME `/verify` PASS 5/5 (the REAL `sovereign-node` binary over the REAL mTLS surface, DEFAULT checkpoint interval so the durable tier stays EMPTY: insert→IMMEDIATE query→200 + IMMEDIATE range→200 + RED never-inserted→404 + `supremum_query_live_source_reads==3` + durable tier EMPTY — `==== DAY-27 VERIFY: PASS ====`). §III GATE green: build/vet(`internal/database`+`internal/telemetry`+`cmd/sovereign-node`)/gofmt clean (the `go vet ./...` `unsafe.Pointer` warnings are ALL in `pkg/sync`, PRE-EXISTING — vet count UNCHANGED); fieldalignment ZERO new debt (database 11==11, telemetry 1==1, metrics 0==0 — the `LiveEvent` DTO is fieldalignment-clean, the `ResolverConfig`/`Resolver` fields add ZERO findings); 5-FILE FROZEN md5 byte-identical (`835350a8`/`ed9132a2`/`47d2796a`/`590af228`/`b1beba1e`); verifier-side byte-UNCHANGED (`3c1b4a8f`/`22c36f61`/`d0830b43c`/`8fcc149b` — Day 27 touches NONE; the seam is an interface + a cmd-layer adapter, NOT a durable-read edit); `TestGate_FrozenMD5`+`TestGate_UntouchedFrozenAndOutOfScope`+`TestGate_GearHonesty`+`TestBench_FrozenMD5` GREEN (the authoritative `pkg/receive` gate); `TestHotPathZeroAllocations` GREEN (READ path + telemetry, NOT write — the live merge is a per-query DTO slice, NOT a hot-path allocation); the Day-27 teeth GREEN across 4 packages (`go test -run TestTrack27` 2.781s + 0.007s + 0.024s + 0.006s); `-race` per-package clean (the 4-core box constraint; the FULL `internal/database` -race hits the 4-core-box 600s+ timeout on the PRE-EXISTING Day-20 `TestTrack20_T_Equiv_DifferentialFuzz`, NOT a Day-27 regression — ZERO live-source refs in that test). §5 STAYS CONDITIONAL-GO (Day 27 is NOT an E1/E2/E3/E5 verdict-blocker; a READ-path + telemetry fork, does NOT touch the single-writer root-equality qualifier the FROZEN `crdt.go` blocks). ZERO FROZEN touched (the TENTH clean-chain fork after Day-18/19/20/21/22/23.1/24/25/26). SCALE BOUND (disclosed, NOT closed): `engine.State().Get` returns the FULL dot set for the entity (O(dots for THAT entity), NOT O(total entries)) + the EBR-retire accumulates under read-concurrency; an O(1) per-entity live-cursor alternative is a SEPARATE fork (`crdt.go` FROZEN — deferred, ADR-0032 §6.a). Enforced by `internal/database/day27_track27_test.go` + `internal/telemetry/day27_track27_test.go` + `pkg/durability/day27_track27_test.go` + the re-pinned `internal/database/day26_track26_test.go` + `internal/telemetry/day18_track18_test.go` + `internal/telemetry/day21_track21_test.go` + `internal/telemetry/day22_track22_test.go` + `internal/telemetry/day24_track24_test.go` + `internal/telemetry/day25_track25_test.go`.- [ADR-0033: The TOKI Closure — the `internal/temporal_store/` Ghost Exorcism + the Blueprint Track 4.3 Won't-Fix (Day 28)](0033_toki_closure_ghost_exorcism_track28.md) — **ACCEPTED (Day 28, 2026-08-11)**: closes the blueprint Track 4.3 "TOKI Conflict Resolution Integration" ghost — WON'T-FIX (NOT a close-by-implementation; a close-by-REJECTION). `internal/temporal_store/` (10 files, 1576 lines incl. the 21-line README; 1555 `.go` lines) is DELETED as zero-importer dead code (M1 — `rg "hr18vk/supremum/internal/temporal_store"` returns ZERO hits, exit 1). The `go.temporal.io/sdk` (go.mod:25), `go.temporal.io/api` (go.mod:75), + `github.com/gogo/protobuf` (go.mod:54) deps are SURGICALLY dropped from go.mod (a 3-line hand-edit, NOT `go mod tidy`; uuid at go.mod:15 STAYS direct — `go mod why` returns `cmd/sovereign-node`→`go.opentelemetry.io/otel/sdk/resource`→`github.com/google/uuid`; the prior session's policy: keep the live diff minimal, tidy would demote uuid + shuffle ~6 lines). Premise-audit (the FIRST deletion-only fork's discipline): (M1 LOAD-BEARING — the abort gate) `rg "hr18vk/supremum/internal/temporal_store" --glob '*.go'` ZERO hits — no production package imports the ghost; the throwaway-copy probe (`$JOB_DIR/tmp/secopy_day28` — `rm -rf` + `go mod tidy` + `go build ./...` exit 0; `go mod why` "main module does not need" for all 3 deps; `go list -m all | grep temporal|gogo` EMPTY) PROVED the deletion build-safe in advance; the live-tree edit is the surgical 3-line go.mod hand-edit. (M2 LOAD-BEARING — the convergence-law regression) `Join` (`crdt.go:1089`, FROZEN md5 `835350a8`, MANDATE-2 single-writer root-equality qualifier) is MERGE-UNION on `CausalDot` (`perShardMerge` `:1221`, `HashCausalDot` `:1580`) — NO dot dropped; `LWWOperator.Resolve` (`toki.go:193`) returns `(winner, loser, audit)` + the caller DROPS the loser (the `AuditRecord` carries `loser_tx_time`); wiring `Resolve` into `Join` would drop a dot that CANNOT re-merge across a foreign `AdvanceLamportTo` (the Day-8.5 WAL-replay seed, `recovery.go:78`) → CRDT convergence break → DATA CORRUPTION — a regression, NOT an optimization; `Join` is FROZEN, the wiring is NOT possible WITHOUT an unfreeze, + even unfrozen it would be a regression. (M3 — the external-orchestration engine) `workflow.go` + `compactor.go` used `go.temporal.io/sdk`'s `workflow` + `activity` to orchestrate "Zero-GC LSM L0→L1 Compaction" — an EXTERNAL durable-workflow engine for a job the Day-14 `L1Compactor.Compaction` (`internal/database/l1_compactor.go`) ALREADY ships IN-PROCESS (the background scheduler in `cmd/sovereign-node`); the Temporal orchestration was NEVER wired (zero importers). (M4 LOAD-BEARING — the live HAMT read path IS the conflict resolution) `selectLatestDot` (`pkg/mesh/gossip.go:228`, called by `handleGet` at `control.go:357`) + Day-27's `engineHAMTAdapter` (`main.go:1303`, `engine.State().Get`) read the FULL dot set + pick the max-`SystemTime` entry LOSSLESSLY (the loser STAYS in the APPEND-ONLY HAMT — `grep Prune|Reset|Retire` on `crdt.go`/`hamt.go` is empty); the resolution is at READ TIME, lossless; the eager `Resolve` is LOSSY (drops the loser); the live read path Day-27 shipped IS the genuine conflict resolution the blueprint line was reaching for — the ghost is OBSOLETE the moment Day-27 landed. (M5 — uuid stays direct) `go mod why github.com/google/uuid` returns a NON-trivial chain (uuid STILL NEEDED); the surgical 3-line hand-edit keeps the diff to EXACTLY the 3 dropped deps (verified: `git diff go.mod` EXACTLY 3 `-` lines, md5 `4b0f155a...`→`9438c4ef...`, uuid stays direct at line 15, line count 92→89). GATE: `T-TOKI-BUILD-GREEN` (`go build ./...` exit 0 on the live post-deletion tree) + `T-TOKI-DEAD-CODE` (`rg "temporal_store"` exit 1 — the package GONE) + `T-TOKI-GO-MOD-CLEAN` (`go list -m all | grep temporal|gogo` EMPTY; `go mod why` "main module does not need" for all 3; uuid STAYS) + `T-TOKI-NO-FROZEN-TOUCH` (5-FILE FROZEN md5 byte-identical `835350a8`/`ed9132a2`/`47d2796a`/`590af228`/`b1beba1e`; verifier-side byte-UNCHANGED `3c1b4a8f`/`22c36f61`/`d0830b43c`/`8fcc149b` — Day 28 touches NONE; the deletion is a `git rm` + a go.mod hand-edit) + `T-TOKI-RUNTIME-VERIFY` (LOAD-BEARING — the REAL `sovereign-node` binary rebuilt from the post-deletion tree boots + the Day-27 read-your-writes round-trip STILL WORKS: insert→IMMEDIATE query→200 + IMMEDIATE range→200 + RED never-inserted→404 + `supremum_query_live_source_reads==3` + durable tier EMPTY — `==== DAY-27 VERIFY: PASS ====` 5/5; the binary embeds `vcs.revision=40b2118` + `vcs.modified=true`; the ghost was NEVER on the runtime path). §III GATE green: build/vet/gofmt clean (the `go vet ./...` 35 `unsafe.Pointer` warnings are ALL pre-existing in `pkg/sync` FROZEN CRDT, UNCHANGED 35→35 — `internal/temporal_store` had ZERO `unsafe.Pointer` warnings); fieldalignment ZERO new debt (database 11==11, telemetry 1==1, metrics 0==0 — the `temporal_store` structs fell out, the 11/1/0 baseline holds, the deletion is fieldalignment-NEUTRAL); `TestGate_FrozenMD5`+`TestGate_UntouchedFrozenAndOutOfScope`+`TestGate_GearHonesty`+`TestBench_FrozenMD5` GREEN (the authoritative `pkg/receive` gate); `TestHotPathZeroAllocations` GREEN (the deletion touches the hot path NOT AT ALL — `internal/temporal_store` was never on it); the Day-27 teeth GREEN across 4 packages post-deletion (the Day-27 seam is byte-identical; the deletion is read-path-orthogonal); `-race` per-package clean (the 4-core box constraint; the FULL `internal/database` -race hits the 4-core-box 600s+ timeout on the PRE-EXISTING Day-20 `TestTrack20_T_Equiv_DifferentialFuzz`, NOT a Day-28 regression — ZERO `temporal_store` refs). §5 STAYS CONDITIONAL-GO (Day 28 is NOT an E1/E2/E3/E5 verdict-blocker; a DEAD-CODE DELETION + a go.mod dep prune, does NOT touch the single-writer root-equality qualifier the FROZEN `crdt.go` blocks). ZERO FROZEN touched (the ELEVENTH clean-chain fork after Day-18/19/20/21/22/23.1/24/25/26/27 — the FIRST deletion-only fork in the chain, the Phase-4 bookend). Carry-forwards (disclosed, NOT closed): the `go.sum` checksum lines for the 3 dropped deps MAY linger (the build does NOT always rewrite `go.sum` for modules not in the build graph; the stale lines are HARMLESS — a future maintainer `go mod tidy` would clean them; Day 28 does NOT run tidy, the prior session's policy). The live cross-entity tail mega-fork (ADR-0032 §6.b, the OPEN-P1, the streak-breaker) + the O(1) per-entity live-cursor (ADR-0032 §6.a, `crdt.go` FROZEN) + the upper-bound `maxSys` sidecar (ADR-0030 §6.b) + the Fenwick sweep balance (ADR-0025 §6) + the inferrer backoff auto-tuning (ADR-0027 §6.3) + the NEXT ZERO-ALLOC JOIN fork (ADR-0022 §8) remain OPEN. Enforced by `go build ./...` (T-TOKI-BUILD-GREEN) + `rg "temporal_store"` (T-TOKI-DEAD-CODE) + `go list -m all | grep temporal|gogo` (T-TOKI-GO-MOD-CLEAN) + `md5sum` FROZEN 5 + verifier 4 (T-TOKI-NO-FROZEN-TOUCH) + the `verify_day27` runtime harness (T-TOKI-RUNTIME-VERIFY) + `TestGate_FrozenMD5`/`TestGate_UntouchedFrozenAndOutOfScope`/`TestGate_GearHonesty`/`TestBench_FrozenMD5`/`TestHotPathZeroAllocations` (the `pkg/receive` + `pkg/sync` scope + zero-alloc gates) + the Day-27 teeth (the read-your-writes seam byte-identical post-deletion).
- [ADR-0034: Stratified Anti-Entropy Mesh Wiring — the Phase-5 Track-5.1 FIRST Stake (Day 29)](0034_stratified_anti_entropy_mesh_wiring_track29.md) — **ACCEPTED (Day 29, 2026-08-11)**: the TWELFTH clean-chain fork — the SECOND deletion-bearing fork + the FIRST streak-breaker since Day-13. Wires stratified anti-entropy into the mesh sweep: a peer-to-peer StrataEstimator + full-IBLT digest-exchange phase before the per-peer `GenerateDelta`, replacing the O(N×M) oversend (EVERY entry to EVERY peer) with a minimal delta proportional to |A−B|. **THE ARCHITECT'S AMENDMENT (the load-bearing mid-audit M2 refutation):** the dormant primitive `GenerateDeltaStratified` (crdt.go:1934, the Day-23 SkipList.Seek mold — built + unit-tested GREEN + wiring-DORMANT) was REFUTED by direct read — it created `remoteIBLT := NewDynamicIBLT(dEst, 4, seed)` then NEVER populated it (the shard-root loop filled only the LOCAL IBLT); `localIBLT.Subtract(remoteIBLT)` subtracted an EMPTY IBLT → the peel yielded ALL local keys → the stratified delta was byte-identical to oversend for ANY non-empty overlap (NO bandwidth cut — the fork's entire M3 value absent; the dormant unit test only covered the empty-remote case where oversend==stratified by coincidence, so the defect never fired). The fundamental obstacle: the StrataEstimator is a LOSSY XOR-based digest (NOT invertible to the key set); the remote's FULL IBLT MUST come from the wire, NOT be reconstructed from the estimator — the primitive's signature `GenerateDeltaStratified(remoteEstimator *StrataEstimator)` could not supply the remote IBLT. FIX: the mesh's digest exchange sends the remote's FULL IBLT digest on the wire (alongside the SE used only for the dEst sizing hint), + the sweep calls `GenerateDelta(remoteIBLT)` (crdt.go:1603 — the FROZEN, CORRECT set-reconciliation primitive that subtracts the POPULATED remote IBLT + peels the real diff) instead of the broken `GenerateDeltaStratified`. The primitive is DELETED (the D2 leak's only host — `GenerateDelta` has ALWAYS set `participantPoolPtr` at crdt.go:1736, so the leak does not exist on the wired path; the D2 fix + the primitive deletion are the TWO crdt.go changes this unfreeze carries — the single re-pin, `835350a8`→`44f89527`; the Day-18 "no re-pin since Day-13" streak is BROKEN for this physical defect, Architect-authorized). Premise-audit (M1–M6 verified BEFORE code): (M1) primitive existed + tested + dormant — the Day-23 mold, then REFUTED in M2 + deleted; (M2 LOAD-BEARING) the primitive subtracted an EMPTY remote IBLT → byte-identical to oversend, NO bandwidth cut — REFUTED by direct read, the primitive KILLED + `GenerateDelta(remoteIBLT)` wired (the remote IBLT from the wire); the `GenerateDelta` path IS byte-identity-by-construction (the diff is a strict subset; `Join` is MERGE-UNION, FROZEN crdt.go:1089) + delivers the REAL bandwidth cut (T-STRUCE-BANDWIDTH-CUT: 150 vs 600, a 75.0% cut); (M3) the digest exchange is a peer-TLS data-plane round-trip BEFORE the per-peer `GenerateDelta` (a self-diff is a no-op); (M4) the recon agent's `/v1/strata-digest` control-port recommendation was REFUTED by direct read of `gossip_test.go`+`partition_test.go` (both build ONLY PeerSet+Gossiper — NO ControlServer, NO HTTP listener; the mesh is pure peer-TLS data-plane) — the digest exchange rides the peer TLS data-plane (a new mesh-internal digest frame: `WireDigestMagic` discriminator in `wire_v1.go` + `MarshalStrataEstimator`/`UnmarshalStrataEstimator` siblings in `iblt_wire.go` + `HandleDigestFrame` sink in `pkg/receive` + `SetDigestSink` on `PeerSet`); (M5) a digest timeout / malformed digest / peel failure falls back to oversend (CRDT-idempotent `Join` — convergence holds) + fires the 19th SSoT counter `StratifiedAntiEntropyFallback` (the Law V disclosure); the D2 leak (the stratified sibling's `participantPoolPtr` omission) was surfaced + the amendment made it MOOT by deleting the primitive; (M6) the SSoT grew 18→19 (`StratifiedAntiEntropyFallback` in all 4 registry sites; the bridge auto-surfaces it, §0.f — ZERO bridge edit). GATE: 10 teeth GATED+GREEN in `pkg/mesh/day29_stratified_test.go` + the M2 unit test in `pkg/sync/crdt_test.go`: `T-STRUCE-OFF-IS-BYTE-IDENTICAL` (OFF byte-identical Day-28, opt-IN zero-value), `T-STRUCE-BYTE-IDENTITY` (ON root==OFF root, Law II; ON 1 round vs OFF 1, converges-never-breaks), `T-STRUCE-BANDWIDTH-CUT` (LOAD-BEARING — stratified delta=150 vs oversend=600, a 450-entry cut, 75.0% of oversend; Law V NUMBER, |A−B|=150 of a 600-entry set, 1024-bucket digest at 0.59 load, loopback 4c, NOT silicon), `T-STRUCE-FALLBACK-COUNTER-FIRES` (mixed-mode A ON B OFF: A fell back 1×, converged anyway; the 19th SSoT counter fires), `T-STRUCE-SSOT-19` (`Counters()`=19 DISTINCT; the bridge auto-surfaces), `T-STRUCE-FROZEN-REPIN` (crdt.go 44f89527; the other 4 byte-UNCHANGED: crdt_apply.go ed9132a2, envelope.go b1beba1e, schema.capnp 47d2796a, schema.capnp.go 590af228), `T-STRUCE-RACE` (ON converged race-clean under `-race`), `RED-NEUTER` (the D2 bug-inject control — 2000 `GenerateDelta(remoteIBLT)`+`Release` cycles, the pool does NOT dry; the RED arm proves the deleted primitive's D2 leak was REAL, the amendment makes the fix MOOT by deleting the host), `T-STRUCE-M2-CUT-PROVEN` (the M2 bug-inject control — GREEN=150 vs OFF=600 the cut; RED=600==OFF the cut VANISHES under the injected bug, the M2 fix is LOAD-BEARING), `T-STRUCE-WIRE-COST` (the honest-overhead disclosure — the digest frame is 72311 bytes/peer/round: SE 51789 + IBLT 20498 + header 24; the cut wins at 75% overlap NET=2689 B; the saturation limit ~750 entries/node + the near-empty-diff net cost are DISCLOSED). **THE SATURATION LIMIT (the honest physical bound):** the FROZEN `GenerateDelta` builds its LOCAL digest at a FIXED 1024 buckets (crdt.go:1610); the remote IBLT MUST match (Subtract requires identical bucket counts, iblt.go:377); the 1024-bucket IBLT saturates past ~750 keys (peel collapses past ~0.7 load → GenerateDelta falls back to oversend = NO bandwidth cut) — the probe measured total=750 YIELDS 188 (cut holds), total=800 OVERSENDS (saturated); the dEst-sized dynamic digest that lifts the limit is a SEPARATE fork (the `GenerateDigestDynamicWithSeed`+`Cardinality` primitives were probed + REVERTED this fork). §III GATE green: `go build ./...` exit 0; gofmt clean; `go vet` clean on `pkg/mesh`+`pkg/sync`+`internal/telemetry`+`pkg/receive` (the `go vet ./...` 35 `unsafe.Pointer` warnings are ALL pre-existing in `pkg/sync` FROZEN CRDT, UNCHANGED); fieldalignment ZERO new debt (database 11==11, telemetry 1==1, metrics 0==0; the `Gossiper.stratified`+`digestRecv`+`digestWaitTimeout` fields + the `peerDigest` struct + the 19th counter are fieldalignment-clean; the ONE new finding is the `day29Harness` TEST struct, OFF the hot path); 5-FILE FROZEN md5: crdt.go re-pinned `835350a8`→`44f89527` (the D2+M2 combined re-pin, ADR-disclosed streak-breaker; re-pinned across 14 test sites) + the other 4 byte-UNCHANGED; `TestGate_FrozenMD5`+`TestBench_FrozenMD5` GREEN (re-pinned to 44f89527); `TestGate_UntouchedFrozenAndOutOfScope` GREEN (crdt.go EXEMPT — the streak-breaker disclosed in the gate; the other 7 untouched files byte-identical to HEAD); `TestGate_UntouchedFrozenAndOutOfScope_Day19` GREEN (the `pkg/mesh` gate, crdt.go exempted); `TestTrack36_ScopeTooth` GREEN (the per-track scope gate, the Day-29 edited set exempted with disclosure); `TestGate_GearHonesty` GREEN (4c, the honest SCISSORS label — loopback 4c, NOT silicon); `TestHotPathZeroAllocations` GREEN (the mesh sweep + digest exchange are OFF the hot path; the write path untouched); the 10 Day-29 teeth GREEN under `-race` (42.3s, no DATA RACE); `TestPhase25B_DeltaGenZeroGC` GREEN (the deleted-primitive boundary marker fell back to the next-func scan — the stale comment corrected); the full `pkg/sync`+`pkg/mesh`+`pkg/receive`+`pkg/metrics`+`pkg/transport`+`pkg/authorization`+`pkg/durability`+`internal/telemetry`+`internal/database` suites GREEN (the ONE pre-existing failure `TestPhase25D_CASStormShardedRuntime` is the known pkg/sync CAS-retry-inflation 4c-box measurement flake — 1.1875x vs 1.180x ceiling, a 0.0075x margin; CONFIRMED pre-existing via stash+HEAD-run, NOT a Day-29 regression). RUNTIME `/verify` PASS 5/5 (the REAL Day-29 `sovereign-node` binary: insert→IMMEDIATE query→200 + IMMEDIATE range→200 + RED→404 + `supremum_query_live_source_reads==3` + durable EMPTY — the read-your-writes seam byte-identical post-Day-29; the mesh sweep is read-path-orthogonal). §5 STAYS CONDITIONAL-GO (Day 29 is NOT an E1/E2/E3/E5 verdict-blocker; the mesh sweep + digest exchange do NOT touch the single-writer root-equality qualifier the FROZEN `crdt.go` blocks — the crdt.go unfreeze is the D2 leak fix + the primitive deletion, NOT a `Join`/`perShardMerge` edit). ONE FROZEN touched (crdt.go — the D2+M2 re-pin, ADR-disclosed streak-breaker; the other 4 byte-UNCHANGED). Carry-forwards (disclosed, NOT closed): the dEst-sized dynamic digest (the saturation-limit lift — unfreeze `GenerateDelta`'s local-digest builder; the `GenerateDigestDynamicWithSeed`+`Cardinality` primitives were probed + REVERTED this fork), the silicon-scale 100-node bandwidth gate (the prompt's explicit "NO AWS this turn" — the bandwidth cut is measured in-process loopback 4c; the named-silicon gate is the NEXT-NEXT fork), the SE wire-cost (51789 bytes/round, 32 strata IBLTs — the dominant overhead; a future fork could send ONLY the remote IBLT + skip the SE). The live cross-entity tail mega-fork (ADR-0032 §6.b) + the O(1) per-entity live-cursor (ADR-0032 §6.a) + the upper-bound `maxSys` sidecar (ADR-0030 §6.b) + the Fenwick sweep balance (ADR-0025 §6) + the inferrer backoff auto-tuning (ADR-0027 §6.3) + the NEXT ZERO-ALLOC JOIN fork (ADR-0022 §8) remain OPEN. Enforced by `pkg/mesh/day29_stratified_test.go` (the 10 teeth) + `pkg/sync/crdt_test.go` (the M2 unit test) + `TestGate_FrozenMD5`/`TestGate_UntouchedFrozenAndOutOfScope`/`TestGate_GearHonesty`/`TestBench_FrozenMD5` (the `pkg/receive` gate, re-pinned to 44f89527) + `TestGate_UntouchedFrozenAndOutOfScope_Day19` (the `pkg/mesh` gate) + `TestTrack36_ScopeTooth` (the per-track scope gate) + `TestHotPathZeroAllocations` (the zero-alloc gate) + `-race` per-package + the `verify_day27` runtime harness.

- [ADR-0035: PKI Leaf Rotation + CRL Revocation — the Phase-5 Track-5.2 FIRST Stake (Day 30)](0035_pki_leaf_rotation_crl_revocation_track30.md) — **ACCEPTED (Day 30, 2026-08-11)**: the THIRTEENTH clean-chain fork (the SECOND security-gate wiring fork after Day-1). Wires the dormant blueprint Track-5.2 gate on the TLS transport: a node presenting an EXPIRED OR REVOKED leaf is REJECTED at the handshake (BOTH-sides mTLS) + a zero-downtime leaf rotation every 30 days (the automated trigger). The M2(a) probe VERIFIED the EXPIRED claw is INHERITED (default Go-tls chain validation rejects an expired leaf BEFORE the VerifyPeerCertificate callback — the callback runs AFTER normal verification per the Go docs; T-PKI-EXPIRED-REJECTED proves the inheritance, NOT a new-work claim). The REVOKED claw is the load-bearing new work: the VerifyPeerCertificate callback consults a CRL (revokedSerials map) + fires the CertRevokedRejected counter. The M3 TRIPLE: a SIGHUP reloads leaf + CA + CRL (NOT just the leaf — the pre-Day-30 :126 behavior), each INDEPENDENT + atomic under the RWMutex, each never trust-degrades on a failed reload. The CA swap uses GetConfigForClient (the ONLY dynamic-CA hook — ClientCAs/RootCAs are static; the per-connection config reads the live pool under the RLock). The M2(b): the CRL is serial-scoped NOT CA-scoped (T-PKI-SIBLING-NOT-REVOKED — revoking one leaf does NOT revoke the CA's siblings). OPT-IN defaults (empty --crl-path + --cert-rotation-enable=false = byte-identical Day-29). 10 teeth (T-PKI-REVOKED-REJECTED, -SIBLING-NOT-REVOKED, -EXPIRED-REJECTED, -CA-HOT-RELOAD, -CRL-HOT-RELOAD, -ROTATION-TRIGGER, -OFF-IS-BYTE-IDENTICAL, -LEAF-ROTATION, -SSOT-21, -COUNTERS-FIRE) byte-proven on loopback. SSoT 19->21 (CertRevokedRejected + CertRotationTriggered, modeCounters; the gauge count STAYS 3). §III gate GREEN: build/gofmt/vet clean; FROZEN 5-file md5 GREEN (NONE of the 5 FROZEN touched — the Day-29 44f89527 streak PRESERVED, NO streak-breaker this fork); scope gate GREEN (the track36ExemptDay30 map: certgen.go, tls_transport.go, day30_track30_test.go, day29_stratified_test.go); fieldalignment ZERO new debt (the TLSConnections reorder packed it down — the pre-existing 88->56 finding GONE too); HotPath zero-alloc + GearHonesty GREEN (PKI work OFF the hot path); 9 Day-30 teeth + 3 Day-1 teeth GREEN; all per-package suites GREEN (the 3 full-sweep failures classified: scope exemption fixed; TestPhase25A1 + TestReceiver_AcceptDedupBaitNegativeControl_4c are load-induced 4c-box measurement flakes, pass in isolation). RUNTIME /verify PASS 7/7 (harness $JOB_DIR/tmp/verify_day30.go, D30_BINARY env — the FULL mTLS control-port drive, NOT the insufficient selftest 4/4): the REAL Day-30 binary boots, /metrics surfaces BOTH new PKI counters (the §0.f auto-surface; PRESENCE not value — a selftest boot fires NEITHER but the series MUST appear so the bridge enumerates the counter the cmd wiring binds). Carry-forwards (disclosed, NOT closed): silicon-scale CRL-consult latency gate (the 100-node run); operator-path automated rotation (the KMS/HSM-backed minter via the RotationMinter seam); delta-CRLs / a Bloom-filter fast-path; OCSP stapling.
- [ADR-0036: Post-Quantum Transport Readiness — the X25519MLKEM768 KEM Proof + the Hybrid (Ed25519 + ML-DSA-65) Signature Verify (Day 31)](0036_post_quantum_transport_readiness_track31.md) — **ACCEPTED (Day 31, 2026-08-11)**: the FOURTEENTH clean-chain fork (the THIRD security-gate wiring fork after Day-1 + Day-30). PROVES the engine's TLS transport ALREADY negotiates the X25519MLKEM768 hybrid post-quantum key exchange (the M2 "prove, NOT enable" posture — Go 1.24+ advertises MLKEM by DEFAULT; the engine's ServerConfig/ClientConfig set NO CurvePreferences, so two Go 1.24+ peers ALREADY negotiate the PQ KEM — EMPIRICALLY PROVEN on the 4c Graviton loopback box: DEFAULT config → CurveID==4588; forced [X25519] → 29 the BUG-INJECT control; GODEBUG=tlsmlkem=0 → 29 the stdlib knob), wires the operator-visible disclosure counter (PQHandshakeNegotiated — Law V; fires IFF the negotiated CurveID==X25519MLKEM768, NOT on every handshake — PQ-KEM-only), and wires the hybrid (Ed25519 + ML-DSA-65) signature verify (VerifyCRDTFrame_Hybrid — the M3 defense-in-depth BOTH-required gate; the EITHER-or gate is the defense-in-depth INVERSION the T-PQ-HYBRID-EITHER-OR-CONTROL BUG-INJECT tooth PROVES the BOTH gate rejects — Day-25 Law 5). The M4 4c ML-DSA-65 verify bench is RECORDED (73,662 ns/op ~73.7µs, UNDER the 100µs threshold, 0 B/op, 0 allocs/op, GOMAXPROCS=4; the 32c gate is carry-forward — AWS). OPT-IN (the Day-19/23/29/30 precedent): --hybrid-verify (default false) keeps the receive path on the classical-only VerifyCRDTFrame seam — byte-identical Day-30; the PQ-KEM counter is wired unconditionally (the KEM disclosure is the transport seam, NOT the signature verify — the SetRevocationReporter precedent). THE HONEST NOT-YET: the hybrid SIGN (a frame carries BOTH sigs) is a FUTURE fork — the CRDT-delta wire shape change; the v1 envelope carries ONLY the Ed25519 originSig + the Directory carries NO peer ML-DSA-65 pubkey, so under --hybrid-verify a v1 frame is REJECTED (the STRICT mode — a hybrid verifier NEVER accepts a classical-only frame; BOTH is the contract). Day 31 wires the VERIFY + the KEM proof, NOT the production sign. The pq_mldsa.go file is PROMOTED from the pq_preview build tag to the DEFAULT build (the VerifyCRDTFrame_PostQuantum + SignCRDTFrame_PostQuantum + GeneratePreviewKey65 symbols now reachable; Sign/Verify bodies byte-identical to pre-Day-31 — build-tag-removal ONLY; the file's OWN pre-Day-31 doc named promotion as "a FUTURE track that removes this build tag" — Day 31 IS that track). 14 teeth (8 transport: T-PQ-KEM-NEGOTIATED + the T-PQ-KEM-CLASSICAL-CONTROL BUG-INJECT + T-PQ-KEM-RECORD-HANDSHAKE + T-PQ-KEM-CLASSICAL-NO-FIRE + T-PQ-COUNTER-FIRE + T-PQ-SSOT-22 + T-PQ-DIAL-FIRES-COUNTER + T-PQ-OFF-IS-BYTE-IDENTICAL; 6 identity: T-PQ-HYBRID-VERIFY-DUAL + T-PQ-HYBRID-CLASSICAL-REJECT + T-PQ-HYBRID-PQ-REJECT + T-PQ-HYBRID-NIL-PQ-REJECT + T-PQ-HYBRID-EITHER-OR-CONTROL BUG-INJECT + T-PQ-HYBRID-LEN-MISMATCH) byte-proven on loopback NOT silicon. SSoT 21->22 (PQHandshakeNegotiated, a modeCounter — the gauge count STAYS 3) in all 4 registry sites (the Day-21 fill discipline); the bridge auto-surfaces it (§0.f — ZERO bridge edit). §III gate GREEN: build/gofmt/vet clean (the 35 unsafe.Pointer warnings are ALL pre-existing in pkg/sync FROZEN CRDT, UNCHANGED 35->35); FROZEN 5-file md5 GREEN (NONE of the 5 FROZEN touched — crdt.go 44f89527 streak PRESERVED, NO streak-breaker this fork; verifier-side 4 byte-UNCHANGED); scope gate GREEN (the track36ExemptDay31 map: pq_mldsa.go, hybrid_verify.go, hybrid_verify_test.go, tls_pq.go, tls_pq_test.go, tls_transport.go, receiver.go, day29_stratified_test.go); fieldalignment ZERO new debt (NET -2 — the pqHandshakeReporter func() field on TLSConnections packed the struct down, REDUCING the finding count 79->77); HotPath zero-alloc + GearHonesty GREEN (the PQ work is OFF the hot path — handshake-time + verify-time, NOT the 57M ops/s insert path); 14 Day-31 teeth GREEN under -race; Day-30 PKI teeth + Day-29 STRUCE teeth GREEN (the SSoT re-pins 21->22 hold); the SSoT teeth across day18/21/22/24/25/26/27/29/30 RE-PINNED 21->22; all per-package suites GREEN. RUNTIME /verify PASS (harness $JOB_DIR/tmp/verify_day31.sh, D31_BINARY env): the REAL Day-31 binary boots, /metrics surfaces supremum_pki_pq_handshake_negotiated (the §0.f auto-surface, PRESENCE not value), the read-your-writes seam is byte-identical post-Day-31 (insert->IMMEDIATE query->200 + IMMEDIATE range->200 + RED->404), and the mTLS control-port handshake negotiates X25519MLKEM768 (the runtime PQ-KEM proof on the REAL binary). Carry-forwards (disclosed, NOT closed): the hybrid SIGN (the CRDT-delta wire shape change — the FROZEN envelope.go/crdt.go seam is the HONEST question a future fork answers); the silicon-scale 100-node PQ-KEM gate (the Day-29 NO AWS this turn precedent); the server-side PQ counter firing seam; the 3309-byte ML-DSA-65 sig wire-cost (51.7x vs Ed25519 — the pqecobench SIZE gate); the operator-path peer-ML-DSA-65-pubkey provisioning.
- [ADR-0037: Hybrid Post-Quantum SIGN-WIRE — the ShipBatchHybrid (Ed25519 + ML-DSA-65) Batch Sign + the HybridEnvelope Frame + the 4-Way Dispatch + the 23rd SSoT Counter (Day 32)](0037_hybrid_pq_sign_wire_track32.md) — **ACCEPTED (Day 32, 2026-08-11)**: the FIFTEENTH clean-chain fork (the FOURTH security-gate wiring fork after Day-1 + Day-30 + Day-31). Wires the SIGN half of the hybrid post-quantum moat (the Day-31 ADR-0036 wired the VERIFY half + disclosed the honest NOT-YET: under --hybrid-verify EVERY v1 frame is REJECTED because the v1 wire carries ONE Ed25519 sig + the Directory carries ONE ed25519.PublicKey). Day 32 closes that NOT-YET: ShipBatchHybrid signs the marshaled CRDTDeltaBatch wire under BOTH Ed25519 + ML-DSA-65 over the SAME 120-byte SHAKE256 pad (the M1 amortized pad — ONE PQ sign per BATCH, NOT per delta), the HybridEnvelope frame carries BOTH sigs, the identity Directory is provisioned with BOTH pubkeys (RegisterPQ + LookupBoth), the DispatchFrame 4-way router routes the hybrid magic to HandleHybridFrame, VerifyBatchHybrid is the BOTH-required batch verify gate, + the HybridFrameAccepted counter is the 23rd SSoT (the operator-VISIBLE proof the moat is in USE, not just wired). OPT-IN (the Day-19/23/29/30/31 precedent): --hybrid-sign (default false) keeps the self-originated delta path on the v1 BatchEnvelope — byte-identical Day-31 (NO hybrid frame produced or accepted; counter stays 0); --hybrid-verify (Day-31, default false) keeps the receive path on the classical VerifyCRDTFrame seam. **THE M2 LOAD-BEARING WIRING FIX (the teeth caught it):** g.hybridSign on the shipDelta (per-frame) path was a NO-OP because the sweep's `if g.batchSize > 1` guard took the per-frame path when batchSize==0 (the harness default) — T-PQ-HYBRID-E2E-CONVERGES FAILED (roots static, accept=0) + T-PQ-HYBRID-SIGN-VERIFY-OFF FAILED (converged via v1 per-frame, ignoring hybridSign). FIX: --hybrid-sign FORCES shipBatchedDelta (the batch path) regardless of batchSize — the hybrid frame is a BATCH shape (the 120-byte SHAKE256 pad amortizes BOTH sigs over N deltas). After the fix, T-PQ-HYBRID-E2E-CONVERGES PASSED (1000 events in 1 round, acceptA=5 acceptB=5) + T-PQ-HYBRID-SIGN-VERIFY-OFF PASSED (hybrid frames DropVerify'd, no convergence — the symmetric STRICT mode). THE M3 BOTH-GATE (defense-in-depth, carried from Day-31): VerifyBatchHybrid returns edOK && pqOK (short-circuit AND — the cheap classical gate first; the 73.7us PQ verify runs only if classical passed; the T-PQ-HYBRID-EITHER-OR-CONTROL BUG-INJECT tooth proves the BOTH gate is load-bearing — the EITHER-or is the defense-in-depth INVERSION). THE STRICT-MODE SYMMETRY (the M5 orthogonality): a hybrid verifier NEVER accepts a v1 single-sig frame (T-PQ-HYBRID-VERIFY-REJECTS-V1) + a non-hybrid verifier NEVER accepts a BOTH-sig hybrid frame (T-PQ-HYBRID-SIGN-VERIFY-OFF) — the two flags are INDEPENDENT + each side rejects the cross-class frame. 14 teeth (6 mesh+receiver E2E: T-PQ-HYBRID-E2E-CONVERGES + T-PQ-HYBRID-E2E-BYTE-IDENTITY + T-PQ-HYBRID-OFF-IS-BYTE-IDENTICAL + T-PQ-HYBRID-SIGN-VERIFY-OFF + T-PQ-HYBRID-DISPATCH-4WAY + T-PQ-HYBRID-SSOT-23; 9 identity hybrid-SIGN: T-PQ-HybridSignThenVerify + T-PQ-HybridSignClassicalReject + T-PQ-HybridSignPQReject + T-PQ-HybridPadDeterministic + T-PQ-HybridPadDistinct + T-PQ-HybridPadLen + T-PQ-HybridSignBadSeed + T-PQ-HybridSignNilPQSk + T-PQ-HybridSignNilBatchWire; 5 Directory BOTH-pubkey: T-PQ-DirRegisterPQBoth + T-PQ-DirLookupBothClassicalOnly + T-PQ-DirLookupBothMiss + T-PQ-DirRegisterPQNilReject + T-PQ-DirRegisterLookupUnchanged; 3 attribution wire-shape: T-PQ-HybridFrameWireShape + T-PQ-HybridFrameMarshal + T-PQ-HybridFrameIsHybrid) GREEN under -race (no DATA RACE — the hybridSign/hybridVerify/onHybridAccept seams are race-free, the single-writer-before-reader discipline holds). SSoT 22->23 (HybridFrameAccepted, a modeCounter — the gauge count STAYS 3) in all 4 registry sites (the Day-21 fill discipline); the bridge auto-surfaces it (§0.f — ZERO bridge edit). §III gate GREEN: build/gofmt/vet clean; FROZEN 5-file md5 GREEN (NONE of the 5 FROZEN touched — crdt.go 44f89527 streak PRESERVED, NO streak-breaker this fork; the hybrid-SIGN layer is a mesh/identity/attribution addition, NOT a CRDT/data-layer change; verifier-side 4 byte-UNCHANGED); scope gate GREEN (the track36ExemptDay32 map: hybrid_sign.go, hybrid_sign_test.go, directory.go, directory_pq_test.go, wire_v1.go, wire_hybrid_test.go, peer.go, gossip.go, batch.go, digest.go, day32_hybrid_test.go, day29_stratified_test.go, receiver.go, tls_pq_test.go, day30_track30_test.go + the 7 telemetry/database SSOT-count teeth); fieldalignment ZERO new HOT-PATH debt (the new structs — HybridEnvelope, Directory.mPQ, NodeIdentity.PQPriv/PQPub, Gossiper.hybridSign, Receiver.onHybridAccept, day32Harness — are ALL off the per-delta hot path with NO contended atomics; the off-hot-path size findings follow the Day-29 day29Harness TEST-struct precedent: disclosed, accepted); HotPath zero-alloc + GearHonesty GREEN (the hybrid SIGN/VERIFY + the frame codec are OFF the per-delta hot path; the PQ sign is per-BATCH, amortized over N deltas); the 9 SSOT-count teeth + the 2 mode-split teeth RE-PINNED 22->23 / 19->20 counters. RUNTIME /verify PASS 6/6 (harness /tmp/verify_day32_run.go, D32_BINARY env): the moat-armed binary (--hybrid-sign + --hybrid-verify) BOOTS + the read-your-writes seam is byte-identical (insert->IMMEDIATE query->200 + IMMEDIATE range->200 + RED->404 + supremum_query_live_source_reads==3 + durable EMPTY) + supremum_hybrid_frame_accepted PRESENT on /metrics (the 23rd SSoT auto-surface, ZERO bridge edit — PRESENCE not value, the counter is 0 single-node by construction; the counter FIRES is proven by the T-PQ-HYBRID-E2E-COUNTER in-process tooth, acceptA=5 acceptB=5). The Day-31 harness re-run on the Day-32 binary PASSES 7/7 (the read seam + the PQ-KEM proof + the 22nd counter UNCHANGED — NO regression). THE HONEST NOT-YET: the OOB peer-pubkey provisioning is a FUTURE fork (Day 32 assumes a statically-provisioned Directory; the directory-gossip fork is the NEXT PQ fork); the cmd's mesh peer-dial loop is the Day-2 stub ("dial loop pending Day 2" — a pre-existing codebase gap, NOT a Day-32 defect; the 2-node BINARY mesh cannot converge until that lands, so the runtime /verify is single-node + the E2E USEFUL proof is the in-process day32Harness). Carry-forwards (disclosed, NOT closed): the OOB peer-pubkey provisioning (the directory-gossip fork); the cmd's mesh peer-dial loop (the Day-2 stub); the silicon-scale 100-node hybrid-mesh bandwidth gate (the 73.7us PQ sign per batch at 32c + the 3309-byte ML-DSA-65 sig wire-cost, 51.7x vs Ed25519); the operator-path hybrid SIGN via a KMS/HSM minter (the RotationMinter precedent); the delta-CRL/Bloom fast-path for the PQ cert revocation; the hybrid SIGN on the relay/foreign path (the self-origin boundary — a relayer CANNOT re-origin-sign a foreign delta under EITHER sig).
- [ADR-0038: Wire-Protocol + CRDT-Apply Fuzz Harness — the Native `go test -fuzz` No-Panic Anchor for the 4-Way Dispatch + the 5 Ingest-Path Unmarshalers + the Committed Seed Corpus + the Bug-Inject Control + the Desync-Discipline Byte-Identity Tooth (Day 33)](0038_wire_protocol_fuzz_harness_track33.md) — **ACCEPTED (Day 33, 2026-08-12)**: the SIXTEENTH clean-chain fork (the FIRST harness fork; the FIRST fork that ships ZERO non-test source — a test-tier package + a test-file exemption map ONLY). Ships a NEW test-tier package `pkg/security/fuzz/` (8 Go source files: doc.go + 7 *_test.go — the 8th, bug_inject_default_test.go, added post-/ruthless-auditor as the build-tag-conditional pair for the byte-equality tooth) — the native `go test -fuzz` harness against the FOUR-way `DispatchFrame` router (pkg/mesh/digest.go:176) + the FIVE ingest-path unmarshalers under it (`attribution.UnmarshalRelayEnvelope`/`UnmarshalBatchEnvelope`/`UnmarshalHybridFrame` + `sync.UnmarshalStrataEstimator`/`UnmarshalIBLT`). The harness asserts ONE property per input: the call RETURNS WITHOUT PANIC (the M3 oracle — verdict value NOT asserted; the PROCESS STAYS ALIVE). The bug it catches is a PANIC: an out-of-bounds slice index, a nil-deref, an unbounded `make()` (OOM-kill), or an integer overflow that defeats a bounds guard. A native `go test -fuzz` does NOT recover — a panic FAILS the fuzz, which is the proof. The harness makes the receiver's "NEVER panics on adversarial input" docstring contract (receiver.go HandleFrame/HandleBatchFrame/HandleHybridFrame) a FALSIFIABLE BINARY instead of a prose claim — Day-29 + Day-31 + Day-32 each ADDED a new magic + a new unmarshaler to that SAME dispatch surface WITHOUT a machine-driven adversarial input ever testing the claim. **THE M1 frameSink REACHABILITY (the load-bearing correction):** `frameSink` is UNEXPORTED (pkg/mesh/peer.go:174) but `DispatchFrame` is EXPORTED + takes `recv frameSink` — reachable from `pkg/security/fuzz` via Go's STRUCTURAL interface conformance (`nopSink` with the 3 matching methods satisfies `frameSink` WITHOUT naming it; proven at COMPILE TIME by the `DispatchFrame(..., nopSink{}, ...)` call). **THE M6 COUNTER DECISION: SKIP — SSoT STAYS 23.** A panic-firing counter needs a `recover()` in the production path (Law IV violation — M2 verified ZERO recover in the receive path by grep; NO recover guard is added — it would MASK the crash the harness surfaces). A production-firing `WireMalformedDropped` counter is a DIFFERENT feature fork (touches receiver.go + main.go + registry.go 4 sites). M6's own rule: a test-only counter is NOT an SSoT disclosure. No counter ships. **THE BUG-INJECT CONTROL (T-FUZZ-BUG-INJECT-CONTROL — the Day-25 Law 5 / Day-31 KEM-CLASSICAL-CONTROL mold):** a COPY of `UnmarshalIBLT` with the bounds guard DELETED (build-tagged `fuzzbuginject` so the bug copy NEVER ships in prod) PANICS on a short seed (out-of-bounds slice index, recovered); the REAL `UnmarshalIBLT` returns `ErrMalformedDigest` ("short bucket array: got 18 bytes, want 78 for 3 buckets") on the SAME seed. The contrast PROVES the harness is NOT a tautology — a fuzz that finds NO panic on the real unmarshalers is a genuine "no crash found" result, NOT a no-op runner. **THE DESYNC-DISCIPLINE BYTE-IDENTITY TOOTH (T-FUZZ-CORPUS-BYTE-IDENTITY — added POST-/ruthless-auditor):** the §34 /ruthless-auditor finding — the original `TestSeedCorpusIsValid` tooth (T-FUZZ-CORPUS-REPRODUCIBLE) enforced only NO-PANIC, NOT the builder↔corpus byte-equality the seeds_test.go §1 docstring ORIGINALLY (wrongly) claimed it did. The NEW `TestSeedCorpusMatchesBuilders` tooth closes that enforcement gap: for each target it re-derives the seed list from the builder aggregator + asserts the on-disk corpus files match COUNT + per-index BYTE-EQUALITY (numeric-index sort — lexical sort mis-orders seed-10.txt before seed-2.txt, the trap the first draft fell into). BUG-INJECT-PROVEN: mutating `validIBLT` 80→128 buckets without corpus regen → the tooth FAILS (`FuzzUnmarshalIBLT — BYTE desync at index 0: builder=2578B vs corpus=1618B`); the desync discipline is ENFORCED, not asserted. Default build: 36 byte-matched seeds (6 default targets; FuzzBugInjectControl skipped — its builder is build-tagged); `-tags fuzzbuginject`: 42 byte-matched. **THE 32-bit-build length-bomb RESIDUAL (M7, disclosed NOT patched):** `UnmarshalIBLT` (iblt_wire.go:269) + `UnmarshalRelayEnvelope` (envelope.go:563) cast wire `uint32` length fields to `int`; on a 32-bit build `int(uint32(0xFFFFFFFF)) == -1` DEFEATS the bounds guard → OOM-kill. On the engine's 64-bit target (`int`==int64) the product CANNOT overflow → the guards HOLD → NO panic. The engine targets 64-bit exclusively (CLAUDE.md Graviton arm64); a 32-bit hardening is a SEPARATE fork. **THE COMMITTED CORPUS (M4, Law II):** 42 seeds across 7 targets (Dispatch 11 + Relay 6 + Batch 4 + Hybrid 4 + Strata 5 + IBLT 6 + BugInjectControl 6), TRACKED not git-ignored, `go test fuzz v1\n[]byte("...")` format with `\xNN` escapes via `strconv.Quote` (byte-faithful round-trip — independently verified: the 51,789-byte StrataEstimator seed round-trips byte-identical through Quote→Unquote→Unmarshal). **§III GATE GREEN (TRIPLE-REVIEWED — /code-review + /ruthless-auditor dispatched, 3 adversarial reviewers: correctness/security + reproducibility/fidelity + scope/honesty; the reproducibility reviewer CAUGHT the byte-equality enforcement gap, which was then FIXED + the fix bug-inject-PROVEN):** build clean; vet clean (default + `fuzzbuginject`); gofmt clean; fieldalignment NET-NEUTRAL (the lone `corpusPanicError` finding fixed by the `val,target` field reorder 32→24B, manual per the memory `fieldalignment_fix_is_destructive`); FROZEN 4 md5 byte-identical (`crdt.go` 44f89527 + `crdt_apply.go` ed9132a2 + `schema.capnp` 47d2796a + `schema.capnp.go` 590af228 — the Day-29 `44f89527` streak PRESERVED, NO streak-breaker; the harness is a CALLER of the unmarshalers + DispatchFrame, NOT a modifier of the CRDT/data layer; the scope reviewer ran a NEGATIVE CONTROL — tampering pkg/durability/wal.go (in no exempt map) produced a real FAIL, proving the scope tooth genuinely enforces); HotPath + GearHonesty GREEN (the harness is test-tier, OFF the hot path; 4c loopback NOT silicon); `TestTrack36_ScopeTooth` GREEN (the Day-33 edited set exempted via `track36ExemptDay33` — the 7 Go source files explicit + the 42 corpus files via `track36ExemptDay33Prefix = "pkg/security/fuzz/testdata/"`, scoped to testdata/ so a FUTURE fork's new .go source under the package is NOT auto-exempted); the 6 ingest-path packages GREEN standalone. **THE 2 PRE-EXISTING FLAKES CONFIRMED PRE-EXISTING via stash+HEAD-run (NOT Day-33 regressions):** (1) `pkg/sync` `TestPhase25D_CASStormShardedRuntime` — inflation straddles the 1.180x ceiling (1.09–1.21x); PROVEN non-deterministic (3 standalone runs PASS/FAIL/FAIL on the SAME code; Day-33 touches ZERO `pkg/sync` source, `crdt.go` FROZEN byte-identical). (2) the `pkg/receive` 600s timeout under `go test ./...` global load — the known pre-existing `TestReceiver_AcceptDedupBaitNegativeControl_4c` hang (memory `pkg_receive_test_hang`); PASSES standalone (3.1s WITH the Day-33 changes — the `track36_crosscheck_test.go` edit is pure data). **THE HONEST NUMBERS (Law V):** 4 non-fuzz teeth GREEN (corpus no-panic + corpus byte-identity + bug-inject control + compile-time reachability guard); 1,943,415 mutated fuzz execs across the 6 default targets (Dispatch 785,353 + IBLT 501,769 + Relay 302,838 + Batch 323,121 + Hybrid 11,132 + Strata 19,202 — the short-burst gate, NOT the 72-hour production soak); 0 panics found on the real unmarshalers; 1 injected panic CAUGHT (the bug-inject control — the harness is load-bearing); 1 desync CAUGHT in the byte-identity tooth's bug-inject proof (the desync discipline is ENFORCED). 0 non-test source files touched (outside pkg/security/fuzz/ + the track36 test-file edit). 0 FROZEN files touched. 0 new SSoT counters (SSoT STAYS 23). Carry-forwards: the 32-bit-build length-bomb hardening (a 2-line cast-to-int64 fix × 2 unmarshalers, touches CONVENTION-frozen NOT md5-FROZEN, a SEPARATE hardening fork); the production-firing `WireMalformedDropped` counter (wire-attack observability, a DIFFERENT feature fork); the full-gate-stack fuzz (driving the REAL Receiver.HandleFrame NOT just the unmarshal route — needs a real Receiver+Directory+engine, a separate harness); the silicon-scale 72-hour fuzz soak (the operator's deployment gate).
- [ADR-0039: Region-Aware Gossip Data-Plane — the TopologyManager + the Seeded-Deterministic Distinct-Region Fan-Out + the AntiEntropySweep Iteration-Source Swap + the 24th SSoT Counter (Day 34)](0039_region_aware_gossip_data_plane_track34.md) — **ACCEPTED (Day 34, 2026-08-12)**: the SEVENTEENTH clean-chain fork (the FIRST region-aware data-plane fork; the Track-5.1 DATA-PLANE half — the loopback-provable substrate for the Raft metadata-plane arc, a future fork). Swaps `AntiEntropySweep`'s iteration source from the full-mesh `peers.Peers()` (every peer every round — O(N²) at 100 nodes = 10,000 connections/sweep/node) to `topology.Select(ctx)` (intra-region full-mesh + inter-region fan-out-N — the blueprint's O(log N) rounds convergence). Ships a NEW `pkg/mesh` seam: `region.go` (the `RegionTag` uint8 + `sameRegion`/`crossRegion` + the `pickInterRegionFanout` distinct-region tie-break) + `topology.go` (the `TopologyManager` peer registry keyed by `[16]byte` + the seeded-deterministic `Select` via `math/rand/v2` `NewPCG`) + the `gossip.go` iteration-source swap (ONE branch: `topology != nil && regionAware` → `Select(ctx)` else the full-mesh path — the load-bearing wiring change; the per-peer BODY is byte-UNCHANGED) + the `cmd/sovereign-node/main.go` `--region-aware`/`--self-region`/`--region-fanout` flags + the `addr@region` peer-suffix parsing + the ONE-block `SetTopology`/`SetRegionAware`/`SetInterRegionReporter` wiring. OPT-IN (the Day-19/23/29/30/31/32 precedent): `--region-aware` default false = byte-identical Day-33 (the T-TOPO-OFF-IS-BYTE-IDENTICAL tooth). **THE 3 BUGS THE TEETH CAUGHT + FIXED (the load-bearing corrections):** (1) the DETERMINISM BUG — the first `Select` built the cross-region candidate list via MAP ITERATION (NON-deterministic order in Go), so the same seed yielded a DIFFERENT selection per call (a Law IV violation — a flaky selector); T-TOPO-DETERMINISTIC caught it; FIX: `sortInterCandidates` sorts the inter-region candidates by `[16]byte` (lockstep with their regions) BEFORE the Fisher-Yates shuffle, so the same seed yields the same output EVERY run. (2) the DISTINCT-REGION FAN-OUT BUG — the first `pickInterRegionFanout` was a partial Fisher-Yates over CANDIDATE PEERS (picked fan-out-N peers, possibly all in the SAME region — the docstring claimed "prefer cross-region diversity" but the implementation did NOT enforce it); T-TOPO-ROUND-COUNT caught the divergence (the peer-Fisher-Yates variant converged the simulated N=100 mesh in K=7-10 rounds vs the blueprint's O(log_3 100) ≈ 4-5); FIX: group candidates by REGION, pick up to fanout-N DISTINCT regions under the seeded tie-break, then ONE peer per region (a second seeded tie-break) — the distinct-region variant converged in K=3 (well within the ≤5 gate). (3) the PER-NODE SEED BUG — a single global `seed = round+1` made every infected node's `Select` in a given round use the SAME seed → the distinct-region tie-break routed them ALL to the SAME 3 regions → no additional spread (K=10); FIX: each node uses a distinct per-node-per-round seed (`round*N + nodeIndex`) so different nodes fan-out to different regions in the same round (the epidemic-spreading property); the production `SweepLoop` stamps the seed per-sweep + a per-node seed component (the nodeID XOR the round) is the production-honest model. **10 teeth GREEN (the §III gate):** T-TOPO-OFF-IS-BYTE-IDENTICAL (OFF converges 1000 events in 1 round, inter-region counter silent — byte-identical Day-33) + T-TOPO-ON-INTRA-CONVERGES (ON both-region-1, the N=2 no-op, converges in 1 round, counter silent) + T-TOPO-ON-INTER-CONVERGES (ON region-1-vs-2 converges in 1 round, M6 counter fires A=1 B=1 — the inter-region arm IS in use) + T-TOPO-DETERMINISTIC (M2 — same seed → same Select output EVERY run; different seed → different inter subset) + T-TOPO-CONNECTION-CUT (M4 — fan-out 3 selects 4/10 peers, a 60% cut; fan-out 0 selects 1 intra-only) + T-TOPO-RACE (race-free under concurrent SetRegion + SetSeed + Select, the RWMutex discipline, GREEN under `-race` GOMAXPROCS=4) + T-TOPO-SSOT-24 (the 24th SSoT counter PRESENT + named + modeCounter, gauge count STAYS 3) + T-TOPO-ROUND-COUNT (M2/M4 the LOAD-BEARING headline — a SIMULATED N=100 mesh, 10 regions, fan-out 3, per-node-per-round seeded, converges in K=3 rounds, gate ≤5; DETERMINISTIC run1==run2==3; full-mesh baseline K=1 BUT 9,900 connections vs fan-out ~1,300) + T-TOPO-NO-FROZEN-TOUCH (M7 — the 4 md5-FROZEN files + envelope.go byte-identical pre-AND-post) + T-TOPO-SUBSTRATE-UNCHANGED (the Day-29/31/32 substrate teeth compile + run GREEN — the build-green is the load-bearing signal). SSoT 23->24 (InterRegionEnvelopesShipped, a modeCounter — the gauge count STAYS 3) in all 4 registry sites (the Day-21 fill discipline); the bridge auto-surfaces it (§0.f — ZERO bridge edit, PRESENCE not value). **§III GATE GREEN:** build/gofmt/vet clean; FROZEN 4 md5 byte-identical (`crdt.go` 44f89527 + `crdt_apply.go` ed9132a2 + `schema.capnp` 47d2796a + `schema.capnp.go` 590af228 — the Day-29 `44f89527` streak PRESERVED, NO streak-breaker this fork; the region-aware layer is a mesh/cmd/telemetry addition, NOT a CRDT/data-layer change); fieldalignment: topology.go reordered (40→32 pointer-bytes, manual per the memory `fieldalignment_fix_is_destructive`); region.go CLEAN; the Gossiper struct's fieldalignment gap is PRE-EXISTING-style (single-instance control struct, NOT a hot-path contended-atomic, NOT a cache-law violation — disclosed); `TestTrack36_ScopeTooth` GREEN (the `track36ExemptDay34` map — the 3 NEW pkg/mesh files + gossip.go + the 11 SSoT re-pin teeth); the 11 SSoT-count teeth + the 2 mode-split teeth RE-PINNED 23->24 / 20->21 counters; the Day-29/31/32 substrate teeth GREEN (NO regression). **THE 1 PRE-EXISTING FLAKE CONFIRMED PRE-EXISTING via stash+HEAD-run:** `pkg/sync` `TestPhase25A1_NodeFreelistSharded` — the CAS-storm cardinality gate straddles the 1.50x ceiling under `go test ./...` global load (1.33x); PASSES 4/4 STANDALONE with the Day-34 changes (Day 34 touches ZERO `pkg/sync` source, the FROZEN files byte-identical). **THE HONEST N=2 NO-OP (the prompt's §III gate names it):** the cmd dial loop uses a PLACEHOLDER zero peerID (the Day-2 honest gap — the peer's real nodeID is unknown until the peer presents its leaf; OOB pubkey provisioning is the Day-35+ AWS arc), so `--region-aware` ON at N=2 in the cmd binary is a NO-OP (the dial loop's zero peerID does NOT match the addr-derived region keys → every peer routes as `RegionUnset` = intra = byte-identical full-mesh). The loopback gate is the SIMULATED N=100 mesh (in-process, REAL peerIDs), NOT the 2-node binary run (the Day-35+ AWS arc). Day 34 ships the SELECTION logic, NOT the provisioning logic. THE 4c HONESTY (Law VI): 4-core loopback NOT silicon; the 100-node wall-time convergence gate is the AWS arc (a simulated round-count is a NUMBER, NOT a silicon proof). Carry-forwards (disclosed, NOT closed): the Raft metadata-plane (Track 5.1b — the data plane is the substrate); the OOB peer-Directory pubkey provisioning (the Day-35+ arc that makes a REAL 2-node binary mesh converge); the 100-node silicon convergence gate (the AWS arc); the 32-bit-build length-bomb hardening (Day-33 carry-forward); the eng.DataDir race + the Fenwick balance (ADR-0021 §6.1 / ADR-0025 §6, carried forward — Day 34 touches NEITHER).
- [ADR-0040: OOB Peer-Directory Pubkey Provisioning — the `--peer-dir` Deterministic Gate + the `--peer-auto-reconcile` Routing Complement + the `ReconnectLoop` Watcher Wiring + the 2-Node Binary Convergence Proof (Day 35)](0040_oob_peer_directory_provisioning_track35.md) — **ACCEPTED (Day 35, 2026-08-13)**: the EIGHTEENTH clean-chain fork (the FIRST provisioning fork; the Track-5.1 DATA-PLANE provisioning complement that retires the Day-2 zero-peerID dial hazard by class-elimination). Day 34 shipped the region-aware SELECTION logic but disclosed the N=2 no-op: the cmd dial loop keyed the peerConn under a PLACEHOLDER zero peerID (the Day-2 honest gap) while the topology keyed the region under `peerIDForAddr(addr)` (a SHA-256 surrogate) → the zero peerID never matched the surrogate → `Publish(real)` missed → the mesh NEVER converged at N=2. Day 35 retires BOTH halves of the Day-2 dial hazard: (1) the PEERID half via the dial-peerID branch (a provisioned peer dials under the REAL nodeID → the topology selector HITS → `Publish(real)` finds the live peer) + (2) the RETRY half via wiring `ReconnectLoop` (peer.go:528 — the PRE-EXISTING primitive whose docstring says "the production binary wires it" but the binary did NOT; the one-shot "dial loop pending Day 2" residual) as a `go peerSet.ReconnectLoop(meshCtx, pa, host, dialPeerID, 1s, 10s)` watcher after each Dial so a peer not yet listening at boot (the inevitable 2-node startup race) re-dials with bounded exponential backoff until up. Ships a NEW `cmd/sovereign-node/provisioning.go` (the `peerDirConfig` struct + the FAILSAFE line parser `parsePeerDir`/`parsePeerDirLine` — a malformed entry is REJECTED with a named-line error, NEVER coerced to zero, the Day-19/23/29 FAILSAFE discipline; the ML-DSA-65 pubkey round-trips via `mldsa.NewPublicKey(mldsa.MLDSA65(), enc)` from a 1952-byte serialized config blob — the FIRST site that reconstructs a directory PQ pubkey from a serialized config) + `applyProvisioning` (`gossiper.RegisterPeer(realNodeID, pub)` the CRDT verification pubkey into the Directory + `dir.RegisterPQ` the ML-DSA-65 pubkey + `topo.SetRegion(realNodeID, region)` the topology re-key under the REAL nodeID — the retirement) + a `pkg/mesh/peer.go` MODIFIED seam (the `autoReconcile` field tail-placed = fieldalignment NET-NEUTRAL + `SetAutoReconcile` + `reconcilePeerID` the leaf-CN hex-decode + the `Dial` reconcile branch) + `cmd/sovereign-node/main.go` MODIFIED (the `--peer-dir` + `--peer-auto-reconcile` flags + the `applyProvisioning` wiring before the dial loop + the dial-peerID branch + the `ReconnectLoop` watcher wiring). OPT-IN (the Day-19/23/29/30/31/32/34 precedent): `--peer-dir ""` + `--peer-auto-reconcile false` = byte-identical Day-34 (the T-OOB-OFF-BYTE-IDENTICAL tooth). **THE PREMISE-AUDIT CORRECTION (the load-bearing finding the binary harness surfaced):** the plan framed Seam B (`--peer-auto-reconcile`) as the "zero-config bonus — a deploy that opts into `--peer-auto-reconcile` alone converges via the handshake." The binary harness RUN 3 PROVED THIS WRONG: without `--peer-dir`, the Directory has NO verification pubkey for the peer (`RegisterPeer` was never called) → the receiver's `Directory.Lookup(originNodeID)` at `receiver.go:436` MISSES → the incoming CRDT delta is `DropVerify`'d → the mesh CANNOT converge. Seam B is a ROUTING COMPLEMENT to Seam A (it re-keys the PeerSet so `Publish` routes under the real nodeID), NOT a standalone convergence path. Convergence REQUIRES Seam A's OOB-provisioned verification pubkey. The 4 committed-source comment sites that repeated the "converges via the handshake" framing were corrected to disclose the DropVerify honestly; the binary harness RUN 3 honest-negative tooth ASSERTS the non-convergence. **M6: SSoT STAYS 24** (the user's choice — NO 25th counter, NO `registry.go` change; the 24th counter `InterRegionEnvelopesShipped` firing A=1 B=1 in the 2-node binary mesh IS the first runtime disclosure — Day 34 could only assert PRESENCE single-node). **8 T-OOB teeth GREEN (the §III gate):** T-OOB-CONFIG-PARSE (parsePeerDir round-trips a 3-peer config incl. ML-DSA-65; 4 malformed-entry REDs REJECTED not coerced) + T-OOB-PROVISION-RETIRES-SURROGATE (the LOAD-BEARING headline — the Day-35 path makes `Select` return the real nodeID → `Publish(real)` HITS; RED the Day-34 surrogate → misses → the no-op reproduces) + T-OOB-RECONCILE (reconcilePeerID hex-decodes the leaf CN → real nodeID; RED a non-hex CN REJECTED — the hex-decode guard is load-bearing) + T-OOB-OFF-BYTE-IDENTICAL + T-OOB-NO-FROZEN-TOUCH (M7 — the 5 md5-FROZEN files byte-identical pre-AND-post, GUARDS FILE EXISTENCE via `os.Stat` the Day-34 lesson — a wrong-path tooth passes vacuously when the path resolves to nothing, + bug-inject-proven) + T-OOB-RACE (race-free under concurrent Dial + reconcilePeerID, GREEN under `-race` GOMAXPROCS=4) + T-OOB-SCOPE (the per-track scope tooth exempts the 6 in-scope Day-35 files + a NEW negative-control: write `// x\n` to an out-of-scope git-tracked file → assert it SURFACES in `git diff --name-only HEAD` + is NOT in any exempt map → the real scope tooth WOULD fire `t.Errorf` — the tooth is load-bearing NOT vacuous, the Day-33 /ruthless-auditor class; reverted via deferred `git checkout`) + T-OOB-APPLY/EMPTY-NO-OP/MISSING-PATH-FAILS (applyProvisioning resolves all 3 pubkeys via LookupBoth; empty --peer-dir = no-op; a non-empty-but-MISSING path FAILS the boot — a deploy misconfiguration MUST be loud). **THE BINARY HARNESS (`verify_day35.go`, NOT committed — the verify SKILL mold extended from verify_day34.go's single-node to TWO sovereign-node binaries):** a SHARED dev CA (the 2-node gotcha — each node's dial must trust the other's leaf; a per-node CA fails as "signed by unknown authority") + the `localhost:port` peer addr (the leaf's DNSNames are `{nodeID, "localhost"}` NOT `127.0.0.1` → the dial's TLS ServerName must be `localhost` or it fails "no IP SANs") + deterministic `--identity-seed` (0xAA×32 / 0xBB×32 → reproducible nodeIDs the mutual `--peer-dir` carries). 3 runs OVERALL PASS: RUN 1 Seam A CONVERGES (insert A → query B 200 + insert B → query A 200 — the headline, the Day-2 hazard RETIRED) + RUN 2 Seam A + B BOTH CONVERGES (the redundant-reconcile composes without regression) + RUN 3 Seam B ONLY does NOT converge (404 DropVerify — the honest-negative tooth PASSES by proving the disclosed gap fires). The 24th SSoT counter fires A=1 B=1 in RUN 1 + RUN 2. **§III GATE GREEN:** build/gofmt clean; vet clean (the 5 `unsafe.Pointer` warnings are PRE-EXISTING at HEAD, in crdt.go FROZEN + hamt_arena.go + chaos/probe.go — NONE in Day-35 touched files); FROZEN 5 md5 byte-identical (`crdt.go` 44f89527 + `crdt_apply.go` ed9132a2 + `attribution/envelope.go` b1beba1e + `schema.capnp` 47d2796a + `schema.capnp.go` 590af228 — the Day-29 `44f89527` streak PRESERVED, NO streak-breaker; the provisioning layer is an identity/routing addition, NOT a CRDT/data-layer change — NOTE Day-35 widens the FROZEN set from 4 to 5 by adding `attribution/envelope.go` to the NO-TOUCH discipline, though it was already FROZEN-by-convention); fieldalignment NET-NEUTRAL (autoReconcile tail-placed absorbs into existing padding; PeerSet stays "104 could be 80" = identical to HEAD); `TestTrack36_ScopeTooth` GREEN (the `track36ExemptDay35` map — the 3 NEW files + main.go + peer.go + the 2 test files); the Day-29/31/32/34 substrate teeth GREEN (NO regression — the 10 T-TOPO teeth GREEN). **THE PRE-EXISTING FLAKE CONFIRMED PRE-EXISTING:** `pkg/receive` `TestReceiver_AcceptDedupBaitNegativeControl_4c` — the 5%-tolerance bench straddles under `go test ./...` global load (261µs vs 248µs); PASSES 4/4 STANDALONE + full pkg/receive suite PASSES isolated (Day 35 touches ZERO receive hot path / Join / dedup — the track36 edit is pure data; the memory `pkg_receive_test_hang` class). THE 4c HONESTY (Law VI): the binary harness runs on the 4-core executor box over loopback TLS 1.3, NOT named silicon; the convergence is a 2-node loopback proof (insert → converge → cross-query 200), NOT a silicon-scale wall-time gate. Carry-forwards (disclosed, NOT closed): the Raft metadata-plane (Track 5.1b); the 100-node silicon convergence gate + the multi-AZ `--peer-dir` distribution (the AWS arc — the harness proves N=2 over loopback, NOT the multi-AZ WAN); the 32-bit length-bomb hardening (Day-33 carry-forward). ADR-0040 written. **POST-COMMIT-PREP /code-review HARDENING (7 review-found bugs + 3 new teeth, the prompt's 8 teeth were GREEN but used persistent loopback conns that NEVER drop so they did NOT catch a stale-DEAD-peerConn class):** a `/code-review` (5 reviewer angles + a verifier) ran over the Day-35 bytes before the commit. The 7 confirmed findings + fixes (the verifier REFUTED one — the "DEAD-branch orphan leaves both real AND zero in `Peers()` forever" claim, code-grounded: the leaf CN is the stable hex nodeID so `reconcilePeerID` succeeds on every reconnect + `ps.peers[real]` is overwritten → the lingering-both outcome is unreachable; dropped): (1) `Dial` liveness guard (`peer.go:302`) — the `existing.conn != nil` guard mis-read a DEAD peer (closed `*tls.Conn` is non-nil) as LIVE → the re-dial skipped → permanent silent partition; FIX: check `pc.done` (the readLoop signal) NOT the conn pointer; (2) `ReconnectLoop` addr-keyed lookup (`peer.go:621`) — the PRE-EXISTING `ps.peers[peerID]` lookup MISSed a reconciled peer (re-keyed under the real nodeID) → the wait skipped → a tight CPU spin; FIX: look up by ADDR; (3) `readLoop` never closes the conn (`peer.go:458`) — only `defer close(pc.done)`; a drop-during-shutdown leaked the conn + fd; FIX: `defer pc.conn.Close()` (LIFO-ordered so `close(pc.done)` runs first); (4) `Dial` DEAD-branch closes under the write-lock (`peer.go:310`) — `tls.Conn.Close()`'s blocking close-notify Write stalls every concurrent `Publish`/`Peers()`; FIX: capture the stale conn under the lock, close it OUT of the lock; (5) `ReconnectLoop` no ctx-recheck before Dial (`peer.go:603`) — a woken-on-shutdown loop dials a canceled ctx (`tls.Dial` is NOT ctx-aware) → installs an un-reclaimed conn; FIX: re-check `ctx.Err()` after the wait; (6) `ReconnectLoop` `time.After` Timer leak (`peer.go:634`) — `time.After` has no `Stop()` handle → a Timer per in-backoff loop leaks on shutdown; FIX: `time.NewTimer` + `Stop()` (+ drain); (7) surrogate region-tag pollution (`main.go:1035`) — the Day-34 `@region` loop keys `peerIDForAddr(addr)` surrogates; `applyProvisioning` adds real-nodeID keys but never deletes the surrogates → `Select` iterates BOTH → the dead surrogate wastes an inter-region fan-out slot; FIX: `applySurrogateRegions` (extracted helper) skips the surrogate for a provisioned addr. Plus 2 consistency WARNINGs (a `--peers` addr not in `--peer-dir` → silent zero fallback; a `--peer-dir` addr not in `--peers` → registered but never dialed — a silent one-way partition) + the nodeID-dedup (`parsePeerDir` now dedups on nodeID too — two lines with the same nodeID + different addrs REJECTED, was: silently shadowed the first's Directory binding → forged deltas). **3 NEW TEETH (the runtime proofs):** T-OOB-RECONNECT-HEALS (`pkg/mesh/day35_oob_test.go` — a 2-node loopback TLS mesh, dial→drop the server conn (natural readLoop io.EOF NOT ClosePeer)→spawn ReconnectLoop→assert HEAL within 8s; RED control in-comment: the OLD guard would return "already live" → no heal → the 8s deadline `t.Fatalf`; PASSING proves the liveness + readLoop-conn-close + addr-keyed + ctx-recheck + timer-stop compose) + T-OOB-SURROGATE-RETIREMENT (`provisioning_test.go` — calls `applySurrogateRegions` the REAL helper, asserts a provisioned peer's region keys under the REAL nodeID NOT the surrogate → `Select` returns the real nodeID not the dead surrogate; RED: empty `provisionedAddr` keys the surrogate → the pollution reproduces) + T-OOB-NODEID-DEDUP (`provisioning_test.go` — same-nodeID-different-addrs REJECTED with a "duplicate peer nodeID" error (the message asserted); RED: distinct nodeIDs ACCEPTED). **POST-HARDENING §III GATE GREEN:** build/gofmt clean; FROZEN 5 md5 byte-identical (44f89527 streak PRESERVED); fieldalignment NET-NEUTRAL (PeerSet "104 could be 80" = HEAD); ALL teeth GREEN (mesh + cmd); race gate GREEN (full `pkg/mesh` 97.9s under `-race`, no detector fires — the readLoop/Dial/ReconnectLoop changes are race-clean); binary harness OVERALL PASS (RUN 1+2 converge 200, RUN 3 DropVerify 404 — no regression from the 7 fixes).
- [ADR-0044: The WAL Group-Commit — the fsync-COUNT closer for the GATE-1 SLO (Day 39)](0044_wal_group_commit_track39.md) — **ACCEPTED (Day 39, 2026-08-15, loopback §III GREEN; silicon Phase 2 EXECUTED — GATE 1 NOT-MET honestly; inject 272.89s→21.87s PROVEN but a deeper receive-side delta-apply blocker surfaced)**: the TWENTY-FIRST clean-chain fork, the **FIRST durability-fork** (Day-38 ADR-0043 was the durable-config closer; Day 39 is the fsync-count cut). Day 38 shipped --wal/--lsm on NVMe + CHECK A/B/C but GATE 1 NOT-MET at ~272.89s inject fsync-time (10K keys → 10 batches of 1000 → AppendMutation PER ENTRY → ONE fsync PER ENTRY = 10000 fsyncs × ~2.1ms ≈ 21s > the 10s SLO). Root cause Day 36/37/38 traced: the binding constraint is the fsync COUNT, NOT the fsync LATENCY. Day 39 collapses N fsyncs → ONE per batch, ADDITIVE on the SAME WAL (the §8 absence-of-fork — `durability.WAL = chaos.WAL` is a type ALIAS, so a method on `*chaos.WAL` is immediately callable as `b.wal.M()`; ONE source of truth, NOT a second WAL). 4 edits + 5 teeth + 1 infra fix: Edit A (`internal/chaos/wal.go`) — `AppendMutations(ms)` writes N records under ONE `w.mu.Lock` + ONE fsync (`w.sync()`); the `sync()` indirection routes the 3 per-record fsync call sites through `w.sync()` (nil `syncHook` → `w.f.Sync()`, byte-identical to HEAD — verified by diff: the ONLY deletions in wal.go are the 3 `w.f.Sync()` lines, matched by 3 `w.sync()` additions; the encode/write/nextSeq++ bodies byte-identical); `syncHook` is a TEST-ONLY fsync spy (`SetSyncHookForTest`, NEVER production). Edit B (`pkg/durability/bridge.go`) — `PutLocals(items)` → N × InsertLocal (stamps dots) → ONE AppendMutations (ONE fsync); per-item physical order byte-identical to PutLocal; on ANY failure → (dots, 0, err) → caller ACKs ALL as 503 (per-batch atomicity). Edit C (`pkg/mesh/gossip.go`) — `InsertLocalEventsBatch(items)` + `BatchItem` (mirrors InsertLocalEvents's two branches; cache.record AFTER the WAL append per item). Edit D (`pkg/mesh/control.go`) — `handleBatchInsert` → 3 phases (per-entry 400 filter → the batch call → status stamping; on success ALL 200, on ANY failure ALL 503); bitemporal stamping per entry mirrors handleInsert verbatim. Edit E (`cmd/day36-gate/main.go`) — `batchSize` 1000 → `*numKeys` → ONE `/v1/batch-insert` → ONE fsync (inject fsync-time ~21s → ~2.1ms + HTTP RTT). Edit F (`phase-03/infra/day36_orchestrator.sh`) — the CHECK-A `grep -c` FALSE-PASS fix: `grep -c` over a glob returns a per-file blob (NOT an integer) → the `-ne` errors → swallowed → `check_a_pass` stays true on a partial fallback (95 ON / 5 OFF); fixed `grep -l | wc -l` (clean integer) — PROVEN by simulation (the old form false-passes a 2/3 partial fallback; the new form correctly FAILs). **§4 THE ACK-GRANULARITY DISCLOSURE (the HONEST semantic change):** Day-37 (`TestBatchInsertWALFailPerEntry`) reported PER-ENTRY 503 (a WAL-failed entry → 503 for THAT entry; the rest 200); Day-39 reports PER-BATCH 503 (a Sync failure → the WHOLE batch un-durable → ALL entries 503 — the WAL atomic-batch model; no subset can be asserted durable). The Day-37 tooth STAYS GREEN under per-batch — verified by running it: the tooth does TWO separate POSTs (all-200 then all-503), never a mixed batch → it is AGNOSTIC to the ACK granularity → both semantics produce the same all-200/all-503 observations. The new `T-GROUP-ACK` tooth DISTINGUISHES the two (posts 5 valid entries + rigs the Sync to fail → asserts ALL 5 are 503; a per-entry regression FAILs it). `/v1/insert` keeps per-entry 503 byte-identical (`PutLocal` unchanged). **5 TEETH (falsifiable + bug-inject-proven, `pkg/mesh/day39_group_commit_test.go`):** T-GROUP-COUNT (`AppendMutations(1000)` = 1 fsync; 1000× `AppendMutation` = 1000 fsyncs — the 1000× cut MEASURED via the spy, NOT asserted; RED a per-mutation fsync inside AppendMutations → 1000 not 1 → FAILs) + T-GROUP-ACK (a rigged Sync → ALL 5 valid entries Code 503; the Sync called EXACTLY once; RED a per-entry 503 regression → pre-failure entries 200 → FAILs the ALL-503 loop) + T-GROUP-DET (a 64-entry batch via AppendMutations replays into a fresh engine and rebuilt MerkleRoot == liveRoot — the determinism contract preserved under group-commit; RED reversed physical order → Merkle mismatch → FAILs; mirrors TestStage6WALRecoveryDeterminism byte-faithfully) + T-DAY39-FROZEN-MD5 (the 5 FROZEN files byte-identical + md5-pinned — the 44f89527 streak preserved; the AppendMutation encode/write/nextSeq++ body byte-identical to HEAD — the §8 absence-of-fork; bogus-path bug-inject) + T-DAY39-SCOPE (production-source diff ⊆ the allowed Day-39 edits + carry-overs; ZERO FROZEN, ZERO crdt.go/hamt.go/crdt_apply.go bleed). The 2 prior SCOPE teeth (Day-36/37) RE-PINNED: `internal/chaos/wal.go` added to both allowed maps (the cumulative-diff precedent Day-36 set for Day-37's files). **§III GATE GREEN (loopback):** build/gofmt/vet CLEAN (the 3 `internal/chaos/probe.go` vet warnings are PRE-EXISTING — proven by stash+vet on the pre-Day-39 tree; Day 39 does NOT touch probe.go); all 5 Day-39 teeth GREEN; regression GREEN (Day-37 batch teeth — the crux; Day-36 T-LOOP teeth; TestStage6WALRecoveryDeterminism — AppendMutation byte-identity; TestHotPathZeroAllocations — InsertLocal FROZEN; full pkg/mesh + pkg/durability + internal/chaos); -race GREEN on the 5 Day-39 teeth; FROZEN-5 byte-identical (44f89527/ed9132a2/b1beba1e/47d2796a/590af228 — the streak PRESERVED through Day 39). **Phase 2 (silicon) EXECUTED 2026-08-15:** 3× c7gd.8xlarge (us-east-1/eu-west-1/ap-southeast-2) with the Day-39 ONE-batch gate binary + the Edit-F CHECK-A fix. VERDICT (Law IV, pasted verbatim): GATE 0 PASS 0.62s; GATE 1 NOT-MET — 99/99 nodes stayed root-zero for 10s + the 30s heal (node 0 holds the 10K keys, `day36-key-0` 404 cross-region incl. intra-region peers); durable-config TRUE (CHECK A 100/100 ON no false-pass, CHECK B WAL=1,348,951B, CHECK C all on /dev/nvme1n1); GATE 3 isolation PROVEN, heal NOT detected in 30s. THE INJECT (Day 39's target) PROVEN: 272.89s → 21.87s (12.5×; loopback `AppendMutations(10K)`=0.58µs/key = the ONE fsync amortized). THE DEEPER BLOCKER: the SSoT counter `supremum_mesh_inter_region_envelopes`=476/914/614 (non-zero + growing) PROVES the gossip transport ships envelopes, but the 10K-key delta NEVER lands in any peer's HAMT — a RECEIVE-SIDE DELTA-APPLY failure (envelopes ship, deltas don't apply), NOT a transport/fsync failure. This is the SAME `99 divergent` blocker Day 36/37/38 hit — always masked by attributing GATE-1 to the inject time. Day 39 closed that secondary inject bottleneck + UN-MASKED the primary receive-side blocker (a Day-40 fork). Teardown clean (SCISSORS). GATE 1 stays NOT-MET — recorded honestly, NOT fabricated. ADR-0044 written. NOTE: ADR-0041/0042/0043 (Day 36/37/38) were committed as forks but their README index lines were not added at the time; this entry restores the index cadence at ADR-0044.
